---
phase: 01-foundation-ci-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-lock.yaml
  - tsconfig.json
  - astro.config.mjs
  - .gitignore
  - .nvmrc
  - vercel.json
  - biome.json
  - .prettierrc
autonomous: true
requirements: [INFRA-01, INFRA-02, INFRA-03, INFRA-05, INFRA-06]

must_haves:
  truths:
    - "A developer can run one command to install and one command to build the project"
    - "The build targets Vercel with static output and no serverless function is created"
    - "All CSS is emitted as external files, never inlined"
    - "RESEND_API_KEY is declared as a server-only secret and requires no value to build"
    - "Outfit and DM Sans are configured to be self-hosted at build time with swap + metrics fallbacks"
  artifacts:
    - path: "package.json"
      provides: "Pinned dependency set, dev/build scripts, engines + packageManager pins"
      contains: "\"packageManager\": \"pnpm@"
    - path: "astro.config.mjs"
      provides: "output static, vercel adapter, inlineStylesheets never, env secret schema, two font families"
      contains: "output: 'static'"
    - path: "vercel.json"
      provides: "Empty deploy config placeholder required by success criterion 5"
      contains: "{}"
    - path: ".gitignore"
      provides: "Build artifact exclusions (node_modules, dist, .vercel, .astro, .lighthouseci)"
      contains: ".vercel/"
    - path: "biome.json"
      provides: "Lint/format config scoped to ts/js/json/css"
    - path: ".prettierrc"
      provides: "Astro-only formatting via prettier-plugin-astro"
  key_links:
    - from: "astro.config.mjs"
      to: "@astrojs/vercel"
      via: "adapter import + adapter: vercel()"
      pattern: "adapter: vercel\\(\\)"
    - from: "astro.config.mjs"
      to: "astro:env/server contract for Phase 5"
      via: "envField.string server/secret"
      pattern: "access: 'secret'"
    - from: "astro.config.mjs"
      to: "Astro Fonts API fontsource provider"
      via: "fonts[] entries with cssVariable --font-display / --font-body"
      pattern: "fontProviders\\.fontsource\\(\\)"
---

<objective>
Stand up the Astro 7 + Vercel project skeleton and every root-level configuration file, with the exact pinned stack, so that `pnpm build` produces a Vercel-targeted static build with zero serverless functions, external-only CSS, a declared server secret schema, and self-hosted font configuration.

Purpose: This is the substrate every other plan in the phase (styles, page, gate scripts, CI workflows) builds on. Nothing else can be verified until the project installs and configures.
Output: `package.json`, `pnpm-lock.yaml`, `tsconfig.json`, `astro.config.mjs`, `vercel.json`, `biome.json`, `.prettierrc`, `.nvmrc`, expanded `.gitignore`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-ci-gate/01-CONTEXT.md
@.planning/phases/01-foundation-ci-gate/01-RESEARCH.md
@.planning/phases/01-foundation-ci-gate/01-PATTERNS.md
@CLAUDE.md

<interfaces>
This plan CREATES the contracts that every later plan and phase consumes. No in-repo analogs exist (greenfield — PATTERNS.md "Greenfield Notice").

Contracts produced here:

- `astro.config.mjs` `fonts[].cssVariable` → injects `--font-display` (Outfit) and `--font-body` (DM Sans) as CSS custom properties. `src/styles/tokens.css` (plan 02) composes `--font-heading: var(--font-display)` and `--font-text: var(--font-body)` on top and MUST NOT redefine the injected variables.
- `astro.config.mjs` `env.schema.RESEND_API_KEY` → the `astro:env/server` contract Phase 5 imports. Declared only; no value, `validateSecrets` left at its default (`false`).
- `build.inlineStylesheets: 'never'` → guarantees `tokens.css` / `base.css` ship as external `<link rel="stylesheet">`, which the plan-03 gate scripts assert.
- `package.json` `scripts` → the exact command names `.github/workflows/ci.yml` (plan 04) invokes.
- Dependency denylist → `scripts/js-weight-check.sh` (plan 03) reads `package.json` dependencies + devDependencies and fails on any of: `react`, `react-dom`, `preact`, `vue`, `svelte`, `@angular/*`, `solid-js`, `framer-motion`, `motion`, `gsap`, `aos`, `lenis`, `locomotive-scroll`, `nprogress`, `@bprogress/*`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold the Astro project and install the pinned dependency set</name>
  <files>package.json, pnpm-lock.yaml, tsconfig.json</files>
  <read_first>
    - `.planning/phases/01-foundation-ci-gate/01-PATTERNS.md` § "`package.json` (config)" (L81-L103) and § "`tsconfig.json` (config)" (L106-L112)
    - `.planning/phases/01-foundation-ci-gate/01-RESEARCH.md` § "Standard Stack" (L86-L137), § "NOT installed in Phase 1 (deferred)" (L110-L118), § "Package Legitimacy Audit" (L139-L159)
    - `CLAUDE.md` § "Technology Stack" version-compatibility table
  </read_first>
  <action>
The repository root is NOT empty (it holds `CLAUDE.md`, `.planning/`, `arquivos de design/`, `.gitignore`), so do not scaffold in place interactively. Run `pnpm create astro@latest` with `--template minimal --typescript strict --no-git --no-install --skip-houston` into a temporary directory OUTSIDE the repo, then copy only `package.json` and `tsconfig.json` into the repo root and delete the temp directory. Do not copy the scaffold's `src/`, `public/`, or `README.md` — plan 02 authors `src/` against the UI-SPEC contract.

Before installing, re-verify each version on the npm registry (`pnpm view <pkg> version`) because RESEARCH.md's "Valid until" window (L928) has elapsed. If a pinned version no longer resolves, install the closest patch within the same major and record the deviation in the SUMMARY.

Install with exact pins (no `^`, no `~` except where noted):
- `dependencies`: `astro@7.3.1`, `@astrojs/vercel@11.0.10`, `@vercel/analytics@2.0.1`
- `devDependencies`: `@astrojs/check@~0.9`, `typescript@5`, `@fontsource-variable/outfit@5.3.0`, `@fontsource/dm-sans@5.3.0`, `@biomejs/biome@2.5.12`, `prettier`, `prettier-plugin-astro@0.14.1`, `@lhci/cli@0.15.1`

Deliberately DO NOT install `sirv-cli`: the blocking Lighthouse gate runs against the real Vercel preview URL (RESEARCH Pattern 3 recommendation, L382), so the local static-server fallback is optional and can be run ad hoc via `pnpm dlx sirv-cli` without adding a committed dependency. Record this discretion call in the SUMMARY.

Do NOT install `resend`, `zod`, `@astrojs/sitemap`, `@upstash/ratelimit`, `@upstash/redis`, or `unlighthouse` — all deferred (RESEARCH L110-L118). Do NOT install any package on the js-weight denylist.

`tsconfig.json` must extend `astro/tsconfigs/strict` and keep the scaffold's `.astro/types.d.ts` include so `astro:env` / `astro:assets` virtual types resolve (Pitfall 5). Commit `pnpm-lock.yaml`. Strip any comment the scaffold emits into `package.json` / `tsconfig.json` (D-04).
  </action>
  <acceptance_criteria>
    - `node -e "const p=require('./package.json');const d={...p.dependencies};if(d.astro?.replace(/^[^0-9]*/,'').split('.')[0]!=='7')process.exit(1)"` exits 0
    - `node -e "const p=require('./package.json');for(const k of ['@astrojs/vercel','@vercel/analytics'])if(!p.dependencies[k])process.exit(1)"` exits 0
    - `node -e "const p=require('./package.json');for(const k of ['@astrojs/check','typescript','@biomejs/biome','prettier','prettier-plugin-astro','@lhci/cli','@fontsource-variable/outfit','@fontsource/dm-sans'])if(!p.devDependencies[k])process.exit(1)"` exits 0
    - `node -e "const p=require('./package.json');const d={...p.dependencies,...p.devDependencies};const bad=Object.keys(d).filter(k=>/^(react|react-dom|preact|vue|svelte|@angular\/|solid-js|framer-motion|motion|gsap|aos|lenis|locomotive-scroll|nprogress|@bprogress\/)/.test(k));if(bad.length){console.error(bad.join(', '));process.exit(1)}"` exits 0
    - `node -e "const p=require('./package.json');const d={...p.dependencies,...p.devDependencies};for(const k of ['resend','zod','@astrojs/sitemap','@upstash/redis','@upstash/ratelimit','unlighthouse','sirv-cli'])if(d[k])process.exit(1)"` exits 0
    - `test -f pnpm-lock.yaml` exits 0
    - `grep -q '"extends": "astro/tsconfigs/strict"' tsconfig.json` exits 0
    - `test -d node_modules/astro && test -d node_modules/@astrojs/vercel` exits 0
  </acceptance_criteria>
  <verify>
    <automated>node -e "const p=require('./package.json');const d={...p.dependencies,...p.devDependencies};const need=['astro','@astrojs/vercel','@vercel/analytics','@astrojs/check','typescript','@biomejs/biome','prettier','prettier-plugin-astro','@lhci/cli','@fontsource-variable/outfit','@fontsource/dm-sans'];const bad=Object.keys(d).filter(k=>/^(react|react-dom|preact|vue|svelte|@angular\/|solid-js|framer-motion|motion|gsap|aos|lenis|locomotive-scroll|nprogress|@bprogress\/)/.test(k));if(bad.length||need.some(k=>!d[k]))process.exit(1)"</automated>
  </verify>
  <done>`package.json`, `pnpm-lock.yaml` and `tsconfig.json` exist at the repo root with the pinned stack installed, no denylisted or deferred package present, and `node_modules/` populated.</done>
</task>

<task type="auto">
  <name>Task 2: Author astro.config.mjs</name>
  <files>astro.config.mjs</files>
  <read_first>
    - `.planning/phases/01-foundation-ci-gate/01-RESEARCH.md` Pattern 1 (L234-L286) — the canonical config shape
    - `.planning/phases/01-foundation-ci-gate/01-PATTERNS.md` § "`astro.config.mjs`" (L59-L78) — the deviations from Pattern 1
    - `.planning/phases/01-foundation-ci-gate/01-UI-SPEC.md` L110 and L389 — font-weight load set reconciliation
    - `.planning/phases/01-foundation-ci-gate/01-CONTEXT.md` `<code_context>` L195-L200 — the real domain
  </read_first>
  <action>
Create `astro.config.mjs` from RESEARCH Pattern 1 (L234-L286), stripping every comment (D-04), with these concrete values:

- Imports: `defineConfig`, `envField`, `fontProviders` from `astro/config`; default `vercel` from `@astrojs/vercel`.
- `site: 'https://agenciadmarques.com.br'` — the real registered domain. Do NOT use `dmarques.com.br` from CLAUDE.md / REQUIREMENTS TRUST-01; that is a stale hypothesis (CONTEXT L196-L200).
- `output: 'static'` and `adapter: vercel()` — bare adapter call. No `webAnalytics` option (legacy, rejected by D-06), no `imageService` (forbidden by PERF-05), no `isr`, no `staticHeaders`.
- `build: { inlineStylesheets: 'never' }` (INFRA-05).
- `env.schema.RESEND_API_KEY: envField.string({ context: 'server', access: 'secret' })`. Declare only — no value, and leave `env.validateSecrets` unset so the default `false` applies and the build does not require the key (nothing imports `astro:env/server` until Phase 5).
- `fonts`: two entries, both `provider: fontProviders.fontsource()`, `subsets: ['latin']`, `fallbacks: ['system-ui', 'sans-serif']`, `optimizedFallbacks: true`, `display: 'swap'`:
  - Outfit → `cssVariable: '--font-display'`, `weights: [400, 500, 600, 700]`, `styles: ['normal']`
  - DM Sans → `cssVariable: '--font-body'`, `weights: [400, 500]`, `styles: ['normal', 'italic']`
  Use the UI-SPEC L389 weight set, NOT RESEARCH Pattern 1's `[300..800]` for Outfit — 300 and 800 are rendered by no design element and every extra weight costs bytes against PERF-04.

Explicitly absent (phase-boundary — do not add): no `security.csp` block (Phase 7), no `experimental.*` flags (graduated to stable in Astro 6), no `integrations: [sitemap()]` (Phase 6), no `markdown` / content config (Phase 2).

Do not create `src/pages/api/*` — the "reserved function boundary" is adapter-only; a stub route would raise the Vercel function count from 0 to 1 prematurely (Pitfall 1).
  </action>
  <acceptance_criteria>
    - `grep -q "output: 'static'" astro.config.mjs` exits 0
    - `grep -q "adapter: vercel()" astro.config.mjs` exits 0
    - `grep -q "inlineStylesheets: 'never'" astro.config.mjs` exits 0
    - `grep -q "site: 'https://agenciadmarques.com.br'" astro.config.mjs` exits 0
    - `grep -A2 RESEND_API_KEY astro.config.mjs | grep -q "access: 'secret'"` exits 0
    - `grep -A2 RESEND_API_KEY astro.config.mjs | grep -q "context: 'server'"` exits 0
    - `grep -c "fontProviders.fontsource()" astro.config.mjs` outputs `2`
    - `grep -q "cssVariable: '--font-display'" astro.config.mjs && grep -q "cssVariable: '--font-body'" astro.config.mjs` exits 0
    - `grep -q "weights: \[400, 500, 600, 700\]" astro.config.mjs` exits 0
    - `grep -c "display: 'swap'" astro.config.mjs` outputs `2`
    - `grep -q "security" astro.config.mjs` exits 1 (no CSP block — Phase 7)
    - `grep -q "imageService" astro.config.mjs` exits 1 (PERF-05)
    - `grep -q "webAnalytics" astro.config.mjs` exits 1 (D-06)
    - `grep -nE '^[[:space:]]*(//|/\*|\*)' astro.config.mjs` exits 1 (comment-free, D-04)
    - `node --input-type=module -e "await import('./astro.config.mjs')"` exits 0
  </acceptance_criteria>
  <verify>
    <automated>node --input-type=module -e "const m=await import('./astro.config.mjs');const c=m.default;if(c.output!=='static')process.exit(1);if(c.build.inlineStylesheets!=='never')process.exit(1);if(c.fonts.length!==2)process.exit(1);if(!c.env.schema.RESEND_API_KEY)process.exit(1)"</automated>
  </verify>
  <done>`astro.config.mjs` loads without error, declares static output + Vercel adapter + external stylesheets + the RESEND_API_KEY server secret + two fontsource font families, contains no CSP/imageService/webAnalytics keys, and has no comments.</done>
</task>

<task type="auto">
  <name>Task 3: Author the root config files and package.json scripts</name>
  <files>vercel.json, biome.json, .prettierrc, .nvmrc, .gitignore, package.json</files>
  <read_first>
    - `.gitignore` (current file, 30 bytes — append only, do not replace)
    - `.planning/phases/01-foundation-ci-gate/01-PATTERNS.md` § "`.gitignore` (modify)" (L135-L141), § "`vercel.json`" (L145-L151), § "`biome.json`" (L116-L122), § "`.prettierrc`" (L126-L131), § "`package.json`" fields (L91-L94)
    - `.planning/phases/01-foundation-ci-gate/01-RESEARCH.md` § "`vercel.json` (Phase 1)" (L691-L697), Anti-Patterns (L462-L471), Runtime State Inventory (L487-L499)
  </read_first>
  <action>
`vercel.json`: contents are exactly `{}` (single line). Success criterion 5 requires the file to exist and be empty. Do NOT add `headers` (HSTS, X-Content-Type-Options, Referrer-Policy, Permissions-Policy, X-Frame-Options) — Phase 7 owns those.

`.gitignore`: APPEND (never rewrite — the existing 30-byte entry for the design thumbnail must survive) the entries `node_modules/`, `dist/`, `.vercel/`, `.astro/`, `.lighthouseci/`. Do not ignore `pnpm-lock.yaml` — it stays tracked.

`biome.json`: Biome 2.5 schema, formatter and linter both enabled, scoped by `files.includes` to `**/*.ts`, `**/*.js`, `**/*.mjs`, `**/*.json`, `**/*.css` and excluding `node_modules`, `dist`, `.vercel`, `.astro`, `.lighthouseci`. `.astro` is not covered by Biome 2 — Prettier owns it, so exclude `**/*.astro`.

`.prettierrc`: declare `plugins: ["prettier-plugin-astro"]` and an `overrides` entry restricting Prettier to `*.astro` with `parser: "astro"`, so Prettier does not fight Biome on the other file types.

`.nvmrc`: the single line `24`.

`package.json` fields:
- `"packageManager": "pnpm@11.15.1"` (verify the locally installed pnpm version with `pnpm --version` and pin that exact string so `pnpm/action-setup` reads it correctly)
- `"engines": { "node": ">=22.12.0" }` — the Astro 7 engine floor
- `"private": true`
- `scripts` (each a single command, INFRA-02): `dev` = `astro dev`, `build` = `astro build`, `sync` = `astro sync`, `check` = `astro sync && astro check`, `format` = `prettier --write "**/*.astro" && biome format --write .`, `lint` = `biome check .`. The `check` script must run `astro sync` first (Pitfall 5 — the `astro:*` virtual types are not generated before a bare `astro check` on a clean checkout).

Every file above must be comment-free (D-04); JSON has no comment syntax, `.nvmrc` carries no `#` line.
  </action>
  <acceptance_criteria>
    - `test "$(tr -d '[:space:]' < vercel.json)" = "{}"` exits 0
    - `for e in 'node_modules/' 'dist/' '.vercel/' '.astro/' '.lighthouseci/'; do grep -qxF "$e" .gitignore || exit 1; done` exits 0
    - `git check-ignore -q pnpm-lock.yaml` exits 1 (lockfile stays tracked)
    - `node -e "JSON.parse(require('fs').readFileSync('biome.json','utf8'))"` exits 0
    - `node -e "JSON.parse(require('fs').readFileSync('.prettierrc','utf8'))"` exits 0
    - `node -e "const p=JSON.parse(require('fs').readFileSync('.prettierrc','utf8'));if(!p.plugins.includes('prettier-plugin-astro'))process.exit(1)"` exits 0
    - `node -e "const p=require('./package.json');if(!/^pnpm@/.test(p.packageManager||''))process.exit(1);if(!p.engines||!p.engines.node)process.exit(1);for(const s of ['dev','build','check','sync'])if(!p.scripts[s])process.exit(1)"` exits 0
    - `node -e "const p=require('./package.json');if(!/astro sync/.test(p.scripts.check))process.exit(1)"` exits 0
    - `test "$(cat .nvmrc)" = "24"` exits 0
    - `pnpm exec biome check . ` exits 0
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('fs');JSON.parse(fs.readFileSync('biome.json','utf8'));JSON.parse(fs.readFileSync('.prettierrc','utf8'));if(fs.readFileSync('vercel.json','utf8').replace(/\s/g,'')!=='{}')process.exit(1);const p=require('./package.json');if(!p.packageManager||!p.engines?.node||!/astro sync/.test(p.scripts.check))process.exit(1);const gi=fs.readFileSync('.gitignore','utf8');for(const e of ['node_modules/','dist/','.vercel/','.astro/','.lighthouseci/'])if(!gi.split(/\r?\n/).includes(e))process.exit(1)"</automated>
  </verify>
  <done>`vercel.json` is `{}`, `.gitignore` excludes all five build-artifact paths while keeping `pnpm-lock.yaml` tracked, `biome.json` / `.prettierrc` / `.nvmrc` exist and parse, and `package.json` carries `packageManager`, `engines.node`, and one-command `dev` / `build` scripts plus a `check` script that syncs first.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local `node_modules` / lockfile | Third-party code enters the build at install time |
| repo source → built client bundle | Anything referenced from config can end up in publicly served assets |
| developer machine → git history | Anything committed is permanently public (repo is public per D-01) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | `package.json` / `pnpm-lock.yaml` dependency tree | mitigate | Exact version pins (no `^`/`~` ranges except `@astrojs/check@~0.9`), `pnpm-lock.yaml` committed, CI installs with `--frozen-lockfile` (plan 04); dependency denylist enforced by `scripts/js-weight-check.sh` (plan 03) |
| T-01-02 | Information Disclosure | `env.schema.RESEND_API_KEY` | mitigate | Declared as `context: 'server', access: 'secret'` so Astro provably excludes it from the client bundle; no value stored in the repo (value is added in Vercel in Phase 5); `security-check.sh` greps the build output for the name and for `re_[A-Za-z0-9]{20,}` (plan 03) |
| T-01-03 | Elevation of Privilege | `adapter: vercel()` surface | mitigate | No `src/pages/api/*` route created, so the Vercel function count stays 0 in this phase; `imageService` left off (no runtime image endpoint); `isr` / `staticHeaders` unset |
| T-01-04 | Information Disclosure | `site` value / third-party origins | mitigate | Fonts self-hosted via the Fonts API (no `fonts.googleapis.com` / `fonts.gstatic.com` request carrying visitor IPs — LGPD-relevant); zero third-party runtime origins configured |
| T-01-05 | Tampering | Build output integrity vs. inline surface | mitigate | `build.inlineStylesheets: 'never'` keeps CSS external, a prerequisite for the strict `style-src` CSP in Phase 7 |
| T-01-SC | Tampering | pnpm installs | mitigate | RESEARCH.md § "Package Legitimacy Audit" (L139-L159) lists all 10 packages with disposition **Approved** — every one is first-party (`astro`, `@astrojs/*`, `@vercel/*`) or a high-download OSS tool; there are no `[ASSUMED]`, `[SUS]`, or `[SLOP]` entries, so no blocking legitimacy checkpoint is required. `sirv-cli` (the only borderline entry) is deliberately NOT installed. Re-verify versions on the registry before install; `sharp`'s native postinstall (transitive via `astro:assets`) is expected and legitimate. |

Residual: none rated High. `security_enforcement` blocks on High only.
</threat_model>

<verification>
```bash
pnpm install --frozen-lockfile
pnpm run sync
pnpm run check
grep -q "output: 'static'" astro.config.mjs
grep -q "inlineStylesheets: 'never'" astro.config.mjs
test "$(tr -d '[:space:]' < vercel.json)" = "{}"
```
`pnpm run check` may report "no pages" style diagnostics until plan 02 adds `src/` — a clean exit is only required once plan 02 lands. Type errors in the config itself must be zero here.
</verification>

<success_criteria>
- `pnpm install --frozen-lockfile` reproduces the tree from the committed lockfile
- `astro.config.mjs` imports cleanly and declares static output, the Vercel adapter, `inlineStylesheets: 'never'`, the `RESEND_API_KEY` server secret, and two fontsource families with the UI-SPEC weight sets
- `vercel.json` exists and is exactly `{}`
- No denylisted UI framework / animation library and no deferred Phase 2/5/6 dependency is installed
- Every file created in this plan is comment-free (D-04)
</success_criteria>

<output>
Create `.planning/phases/01-foundation-ci-gate/01-01-SUMMARY.md` when done. Record: the exact installed versions (and any deviation from the pins), the pnpm version pinned in `packageManager`, and the `sirv-cli` omission rationale.
</output>
