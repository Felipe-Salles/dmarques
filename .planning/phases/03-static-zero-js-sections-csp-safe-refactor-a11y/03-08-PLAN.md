---
phase: 03-static-zero-js-sections-csp-safe-refactor-a11y
plan: 08
type: execute
wave: 3
depends_on: ["03-03", "03-04", "03-05", "03-06", "03-07"]
files_modified:
  - src/pages/index.astro
  - src/styles/tokens.css
autonomous: true
requirements: [SITE-01, SITE-03, SITE-04, SITE-05, A11Y-01, A11Y-02, A11Y-04, ANIM-02, ANIM-07, SEO-02, PERF-05]

must_haves:
  truths:
    - "A home apresenta as 9 seções do design na ordem definida em SITE-01"
    - "A página inteira renderiza com zero JavaScript de cliente"
    - "grep -r 'style=\"' src/ não retorna nada"
    - "Clicar em uma âncora do menu leva a seção para abaixo do cabeçalho, não sob ele"
    - "Existe exatamente um <h1> e os landmarks header/nav/main/footer estão presentes uma única vez cada"
  artifacts:
    - path: "src/pages/index.astro"
      provides: "Composição one-page das 9 seções dentro do BaseLayout"
      contains: "HeroBleed"
    - path: "src/styles/tokens.css"
      provides: "--nav-height com a altura real medida do cabeçalho"
      contains: "--nav-height"
  key_links:
    - from: "src/pages/index.astro"
      to: "src/components/*.astro"
      via: "imports no frontmatter e composição em ordem"
      pattern: "SiteHeader"
    - from: "src/styles/tokens.css"
      to: "âncoras de seção"
      via: "scroll-margin-top em base.css consumindo --nav-height"
      pattern: "--nav-height"
---

<objective>
Compor a home one-page a partir dos 10 componentes das ondas anteriores, substituir o
palpite de `--nav-height` pela altura real medida do cabeçalho, e passar todos os
gates locais da fase de uma vez.

Purpose: até aqui cada componente foi verificado isoladamente por `astro check` e pelo
grep de `style=`; é aqui que SITE-01 (ordem das 9 seções), SITE-05 (offset de âncora) e
a ausência total de JS passam a ser observáveis no HTML construído. É também o último
ponto em que a página pode ser corrigida antes da verificação em preview real.
Output: `index.astro` completo + `--nav-height` definitivo + evidência de gate verde.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-static-zero-js-sections-csp-safe-refactor-a11y/03-UI-SPEC.md
@.planning/phases/03-static-zero-js-sections-csp-safe-refactor-a11y/03-VALIDATION.md
@.planning/phases/03-static-zero-js-sections-csp-safe-refactor-a11y/03-RESEARCH.md
</context>

<interfaces>
Componentes disponíveis, todos sem props, todos já verificados individualmente:

| Componente | Ordem em SITE-01 | id de seção |
|---|---|---|
| `SiteHeader.astro` | cabeçalho (fora de `<main>`) | — |
| `HeroBleed.astro` | 1 | — |
| `ServicesSection.astro` | 2 | `servicos` |
| `ProcessSection.astro` | 3 | `processo` |
| `DifferentiatorsSection.astro` | 4 | `diferenciais` |
| `AboutSection.astro` | 5 | `sobre` |
| `ContactSection.astro` | 6 | `contato` |
| `FaqSection.astro` | 7 | `faq` |
| `CtaFinal.astro` | 8 | — |
| `SiteFooter.astro` | 9 (rodapé, fora de `<main>`) | — |

`BaseLayout.astro` aceita a prop `title` e já emite `<html lang="pt-BR">` (SEO-02),
as `<Font>`, `<Analytics />` e os imports de `tokens.css`/`base.css`.

`SiteHeader.astro` já contém o skip link como seu primeiro nó, apontando para
`#conteudo`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Compor index.astro com as 9 seções na ordem de SITE-01</name>
  <files>src/pages/index.astro</files>
  <read_first>
    - src/pages/index.astro (estado atual: placeholder da Fase 1 que será INTEGRALMENTE substituído — leia antes para saber o que está sendo descartado, incluindo o `<style>` de placeholder)
    - src/layouts/BaseLayout.astro (prop `title` e o que já vem incluído)
    - src/components/SiteHeader.astro (confirmar que o skip link já está dentro dele, para não duplicar aqui)
    - src/components/HeroBleed.astro, ServicesSection.astro, ProcessSection.astro, DifferentiatorsSection.astro, AboutSection.astro, ContactSection.astro, FaqSection.astro, CtaFinal.astro, SiteFooter.astro (confirmar nome de export default e ausência de props)
    - .planning/REQUIREMENTS.md seção "Seções da Página (SITE)" SITE-01 (a ordem canônica)
  </read_first>
  <action>
    Substitua integralmente o conteúdo de `src/pages/index.astro`.

    Frontmatter: importar `BaseLayout` e os dez componentes. Nenhuma lógica.

    Estrutura do body, nesta ordem exata:
    1. `<SiteHeader />` — fora de `<main>`, primeiro filho do `<slot />`. Ele já traz
       o skip link como primeiro nó, então NÃO adicione outro skip link aqui
       (dois links "Pular para o conteúdo" quebram A11Y-01).
    2. `<main id="conteudo">` contendo, em ordem: `<HeroBleed />`,
       `<ServicesSection />`, `<ProcessSection />`, `<DifferentiatorsSection />`,
       `<AboutSection />`, `<ContactSection />`, `<FaqSection />`, `<CtaFinal />`.
       O id `conteudo` é obrigatório — é o alvo do skip link.
    3. `<SiteFooter />` — fora de `<main>`.

    NÃO envolva tudo em um `<div>` com `overflow-x: clip` reproduzindo a linha 28 do
    design. Esse wrapper global é exatamente o que recorta o painel mobile do
    `<details>` (Pitfall 4). Cada componente já contém o recorte de que precisa,
    escopado ao próprio container de imagem/canvas/glow. Se aparecer barra de rolagem
    horizontal, encontre e corrija o elemento específico que transborda em vez de
    aplicar `overflow-x: clip` no ancestral comum.

    O `<style>` da página fica vazio ou inexistente: todo estilo pertence aos
    componentes ou a `base.css`. Remova o bloco `<style>` de placeholder da Fase 1
    (regras de `main` e `h1` centralizados) — ele conflitaria com o layout real.

    Decisão deliberada a registrar no SUMMARY: a collection `cases` (Fase 2, entrada
    "projeto próprio" + capa) NÃO é renderizada nesta fase. SITE-01 lista exatamente
    9 seções e nenhuma delas é portfólio; TRUST-04 adia depoimentos/cases para v1.x.
    Isso é omissão intencional, não esquecimento — deixe escrito para que um revisor
    futuro não tente "corrigir".

    NÃO adicione `<title>`, meta description, canonical, OG, JSON-LD nem favicon: tudo
    isso é Fase 6 (SEO-01/03/04/05/06/07). Passe apenas a prop `title` ao
    `BaseLayout` se quiser um título provisório, ou nem isso — o default do layout já
    serve.

    Zero comentários no arquivo.
  </action>
  <verify>
    <automated>pnpm check && pnpm build && node -e "const h=require('fs').readFileSync('.vercel/output/static/index.html','utf8');const order=['id=\"servicos\"','id=\"processo\"','id=\"diferenciais\"','id=\"sobre\"','id=\"contato\"','id=\"faq\"'];let prev=-1;for(const t of order){const i=h.indexOf(t);if(i<0){console.error('SECAO AUSENTE',t);process.exit(1)};if(i<prev){console.error('FORA DE ORDEM',t);process.exit(1)};prev=i};const one=(re,n,label)=>{const c=(h.match(re)||[]).length;if(c!==n){console.error(label,'esperado',n,'obtido',c);process.exit(1)}};one(/<h1[ >]/g,1,'h1');one(/<main[ >]/g,1,'main');one(/<header[ >]/g,1,'header');one(/<footer[ >]/g,1,'footer');if(!h.includes('id=\"conteudo\"')){console.error('sem main id conteudo');process.exit(1)};if((h.match(/Pular para o conteúdo/g)||[]).length!==1){console.error('skip link duplicado ou ausente');process.exit(1)};if(!h.includes('lang=\"pt-BR\"')){console.error('sem lang pt-BR');process.exit(1)};if(!h.includes('fetchpriority=\"high\"')){console.error('sem fetchpriority high no LCP');process.exit(1)};if(!h.includes('type=\"image/avif\"')||!h.includes('type=\"image/webp\"')){console.error('sem AVIF/WebP');process.exit(1)};console.log('index.html OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm check` e `pnpm build` saem com código 0
    - `.vercel/output/static/index.html` contém os ids `servicos`, `processo`, `diferenciais`, `sobre`, `contato`, `faq` nessa ordem de aparição
    - O HTML tem exatamente um `<h1`, um `<main`, um `<header` e um `<footer`
    - `id="conteudo"` está presente e o texto `Pular para o conteúdo` aparece exatamente uma vez
    - O HTML contém `fetchpriority="high"`, `type="image/avif"` e `type="image/webp"`
    - `grep -c 'overflow-x' src/pages/index.astro` retorna 0
    - `src/pages/index.astro` não contém comentário nem bloco `<style>` com regras de placeholder da Fase 1
    - O SUMMARY registra explicitamente a decisão de não renderizar a collection `cases` nesta fase
  </acceptance_criteria>
  <done>A home compõe as 9 seções na ordem canônica dentro dos landmarks corretos, com um único h1 e um único skip link, e o HTML construído prova a ordem.</done>
</task>

<task type="auto">
  <name>Task 2: Medir a altura real do cabeçalho e fixar --nav-height</name>
  <files>src/styles/tokens.css</files>
  <read_first>
    - src/styles/tokens.css (o valor provisório `--nav-height: 84px` gravado no plano 03-02)
    - src/components/SiteHeader.astro (o padding, altura de conteúdo e breakpoints reais que determinam a altura renderizada)
    - src/styles/base.css (a regra `scroll-margin-top` que consome o token)
    - .planning/phases/03-static-zero-js-sections-csp-safe-refactor-a11y/03-RESEARCH.md Open Question 3 (por que o valor não podia ser decidido antes)
  </read_first>
  <action>
    O token `--nav-height` foi gravado como palpite de 84px no plano 03-02, com
    revisão obrigatória aqui. Agora o cabeçalho existe e pode ser medido.

    Rode `pnpm dev`, abra a home e meça a altura renderizada do elemento `<header>`
    (`getBoundingClientRect().height` no console do DevTools, ou o painel de layout)
    em DOIS breakpoints: um largura de desktop (por exemplo 1440px) e uma largura
    mobile abaixo do breakpoint de 860px definido no `SiteHeader` (por exemplo 390px).

    Se as duas medidas forem iguais dentro de ~4px, grave a maior como valor único de
    `--nav-height`. Se divergirem materialmente (mais de ~8px), declare `--nav-height`
    com o valor mobile no `:root` e sobrescreva-o dentro de uma media query
    `@media (min-width: 860px)` em `tokens.css` com o valor desktop — o
    `scroll-margin-top` de `base.css` continua lendo o mesmo token e passa a se
    adaptar sozinho. Não crie um segundo token.

    Depois de gravar, valide o comportamento real: com `pnpm dev` aberto, clique em
    cada um dos quatro links de âncora do cabeçalho (`#servicos`, `#processo`,
    `#sobre`, `#contato`) e confirme que o título da seção de destino fica VISÍVEL
    abaixo do cabeçalho, não coberto por ele. Repita nos dois breakpoints. Registre no
    SUMMARY as duas medidas, o valor final gravado e se houve media query.

    Não altere nenhum outro token neste plano.
  </action>
  <verify>
    <automated>pnpm build && node -e "const c=require('fs').readFileSync('src/styles/tokens.css','utf8');const m=c.match(/--nav-height:\s*([0-9.]+)px/g);if(!m){console.error('--nav-height ausente');process.exit(1)};console.log('nav-height declarado:',m.join(' | '));const b=require('fs').readFileSync('src/styles/base.css','utf8');if(!b.includes('scroll-margin-top: var(--nav-height)')){console.error('base.css nao consome o token');process.exit(1)};console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `--nav-height` em `tokens.css` não é mais `84px`, OU o SUMMARY documenta que 84px foi confirmado como a medida real (não presumido)
    - `base.css` continua consumindo `var(--nav-height)` em `scroll-margin-top`
    - O SUMMARY contém as duas alturas medidas (mobile e desktop) com a largura de viewport em que foram medidas
    - O SUMMARY confirma, por breakpoint, que os quatro alvos de âncora ficam visíveis abaixo do cabeçalho após o clique
    - `git diff src/styles/tokens.css` mostra alteração apenas em linhas de `--nav-height`
  </acceptance_criteria>
  <done>`--nav-height` carrega a altura real medida do cabeçalho e as quatro âncoras foram verificadas visualmente nos dois breakpoints.</done>
</task>

<task type="auto">
  <name>Task 3: Rodar a bateria completa de gates locais da fase</name>
  <files>(sem alteração de arquivo salvo correções apontadas pelos gates)</files>
  <read_first>
    - scripts/security-check.sh (as sete verificações; a verificacao 2 é o gate de `style=` e a verificacao 3 conta `<style>`/`<script>` inline no HTML construído)
    - scripts/js-weight-check.sh (o orçamento de JS da rota da landing e a proibição de framework de UI / biblioteca de animação)
    - .planning/phases/03-static-zero-js-sections-csp-safe-refactor-a11y/03-VALIDATION.md seções "Sampling Rate" e "Manual-Only Verifications"
    - .planning/security/SECURITY-CHECKLIST.md (o gabarito SEC-07 que o plano 03-09 vai preencher — leia para saber que evidência precisa ser capturada agora)
  </read_first>
  <action>
    Rode, nesta ordem, e capture a saída verbatim de cada um no SUMMARY:

    1. `pnpm format` seguido de `pnpm lint`. Biome cobre `.ts`/`.js`/`.json`/`.css`;
       Prettier com `prettier-plugin-astro` cobre `.astro`. Se algum arquivo for
       reformatado, inclua a reformatação no commit.
    2. `pnpm check` (astro sync + astro check) — deve sair 0 sem erro de tipo.
    3. `pnpm build` — deve sair 0. Confirme na saída que a etapa de geração de imagens
       otimizadas completou e que arquivos `.avif` e `.webp` existem em
       `.vercel/output/static/_astro/`.
    4. `bash scripts/js-weight-check.sh` — a rota da landing precisa ficar abaixo do
       orçamento. Como esta fase embarca ZERO JavaScript de cliente, o peso esperado é
       essencialmente só o beacon de analytics já presente desde a Fase 1. Se o número
       subir de forma inesperada, algum componente introduziu script — encontre e
       remova.
    5. `bash scripts/security-check.sh --ci` — sem `PREVIEW_URL`, as verificações 6 e 7
       vão SKIP; isso é esperado e o plano 03-09 as cobre contra o preview real. As
       verificações 1 a 5 precisam passar, em especial a verificacao 2 (nenhum
       `style=` em `src/`).
    6. Grep manual de sintaxe do design-editor que NENHUM gate existente pega
       (Pitfall 5): `grep -rn 'onClick\|onSubmit\|ref="{{\|{{ \|image-slot\|sc-if' src/`
       — precisa retornar vazio.
    7. Grep de cor não-tokenizada: `grep -rnE '#[0-9a-fA-F]{6}' src/ --include='*.astro'`
       — precisa retornar vazio (hex literal só é permitido em `tokens.css`).
    8. Grep de comentário em arquivo versionado de `src/`: confirme zero ocorrências de
       `/*` e de `//` em posição de comentário nos `.astro` e `.css` novos.

    Se qualquer gate falhar, corrija no componente responsável e rode a bateria inteira
    de novo — não "passe adiante" um gate vermelho anotando-o como conhecido. A única
    exceção legítima são as verificações 6 e 7 do `security-check.sh` em SKIP por
    ausência de `PREVIEW_URL`.
  </action>
  <verify>
    <automated>pnpm lint && pnpm check && pnpm build && bash scripts/js-weight-check.sh && bash scripts/security-check.sh --ci; echo "exit=$?" && test -z "$(grep -rn 'onClick\|onSubmit\|ref=\"{{\|image-slot\|sc-if' src/ || true)" && test -z "$(grep -rnE '#[0-9a-fA-F]{6}' src/ --include='*.astro' || true)" && ls .vercel/output/static/_astro/*.avif >/dev/null && ls .vercel/output/static/_astro/*.webp >/dev/null && echo "GATES OK"</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm lint`, `pnpm check` e `pnpm build` saem com código 0
    - `bash scripts/js-weight-check.sh` passa e o SUMMARY registra o número de KB da rota da landing
    - `bash scripts/security-check.sh --ci` imprime `PASS` nas verificações 1 a 5, incluindo `PASS: verificacao 2 - nenhum atributo style= em src/`; as verificações 6 e 7 em SKIP são aceitáveis apenas por ausência de `PREVIEW_URL`
    - `grep -rn 'onClick\|onSubmit\|ref="{{\|image-slot\|sc-if' src/` retorna vazio
    - `grep -rnE '#[0-9a-fA-F]{6}' src/ --include='*.astro'` retorna vazio
    - Existem arquivos `.avif` e `.webp` em `.vercel/output/static/_astro/`
    - O SUMMARY contém a saída verbatim de `security-check.sh --ci` e de `js-weight-check.sh`
  </acceptance_criteria>
  <done>Toda a bateria local de gates está verde com evidência verbatim capturada, pronta para a verificação em preview do plano 03-09.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTML construído -> CDN da Vercel | artefato imutável servido ao público |
| superfície inline do build -> CSP estrita da Fase 7 | cada `<style>`/`<script>` inline remanescente vira uma exceção que a Fase 7 terá de acomodar |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | HTML construído da home | mitigate | `security-check.sh` verificacao 2 (nenhum `style=` em `src/`) e verificacao 3 (contagem de `<style>`/`<script>` inline no HTML, permitindo apenas os blocos `@font-face` da Astro Fonts API) rodam como gate bloqueante. Esta fase REMOVE superfície inline em vez de adicionar, preparando o `style-src 'self'` da Fase 7. |
| T-03-19 | Tampering | orçamento de JavaScript da rota | mitigate | `js-weight-check.sh` bloqueia framework de UI e biblioteca de animação. Com zero JS autorado nesta fase, qualquer aumento de peso sinaliza script introduzido inadvertidamente — o critério de aceite exige registrar o número medido. |
| T-03-20 | Tampering | grep de sintaxe do design-editor | mitigate | `onClick="{{ }}"` sobrevivente renderizaria como handler inline literal no DOM e nenhum gate existente o pega (o grep de `style=` não cobre). O grep manual dos passos 6 e 7 fecha essa lacuna antes do deploy. |
| T-03-03 | Information Disclosure | artefatos de build em `.vercel/output` | mitigate | `security-check.sh` verificacao 4/5 varre `dist`/`.vercel/output/static` por `RESEND_API_KEY` e por chaves `re_`; esta fase não introduz nenhum segredo, e a verificação continua rodando como regressão. |
</threat_model>

<verification>
- `pnpm lint`, `pnpm check`, `pnpm build` saem com código 0.
- `bash scripts/js-weight-check.sh` passa.
- `bash scripts/security-check.sh --ci` com PASS nas verificações 1 a 5.
- Greps de sintaxe de editor e de hex literal retornam vazio.
</verification>

<success_criteria>
- `index.html` construído mostra as 9 seções na ordem de SITE-01, um único `<h1>`, landmarks únicos, `lang="pt-BR"`, `fetchpriority="high"` e `<source>` AVIF + WebP.
- `--nav-height` carrega altura medida e as âncoras aterrissam abaixo do cabeçalho nos dois breakpoints.
- Toda a bateria local de gates está verde com evidência verbatim no SUMMARY.
- A decisão de não renderizar a collection `cases` está documentada.
</success_criteria>

<output>
Create `.planning/phases/03-static-zero-js-sections-csp-safe-refactor-a11y/03-08-SUMMARY.md` when done
</output>
