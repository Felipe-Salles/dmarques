---
phase: 03-static-zero-js-sections-csp-safe-refactor-a11y
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/styles/tokens.css
  - src/styles/base.css
autonomous: true
requirements: [A11Y-03, A11Y-06, A11Y-07, ANIM-02, ANIM-07, ANIM-08, SITE-05]

must_haves:
  truths:
    - "O anel de foco tem contraste >=3:1 contra #0A0A12, #FFFFFF e #F2F3F6"
    - "Todo token de texto translucido em uso atinge >=4.5:1 contra os tres fundos escuros do projeto"
    - "Ancoras de secao param abaixo do cabecalho em vez de ficarem escondidas sob ele"
    - "Com prefers-reduced-motion reduce o scroll e instantaneo e as keyframes decorativas param"
    - "Sem JavaScript todo elemento [data-reveal] renderiza 100% visivel"
  artifacts:
    - path: "src/styles/tokens.css"
      provides: "Tokens de cor e foco corrigidos para AA mais o token --nav-height"
      contains: "--nav-height"
    - path: "src/styles/base.css"
      provides: "Contratos globais de focus-visible, scroll, reveal inerte e keyframes com guarda de reduced-motion"
      contains: "prefers-reduced-motion"
  key_links:
    - from: "src/styles/base.css"
      to: "src/styles/tokens.css"
      via: "var(--focus-ring-offset) e var(--nav-height)"
      pattern: "var\\(--nav-height"
    - from: "src/styles/base.css"
      to: "Fase 4 IntersectionObserver"
      via: "seletores html.js-ready [data-reveal] e .is-revealed"
      pattern: "html\\.js-ready"
---

<objective>
Corrigir a base de tokens e o CSS global ANTES de qualquer secao ser autorada — as 10
secoes herdam destes dois arquivos, entao corrigir contraste e contratos de movimento
depois seria retrabalho em 14 arquivos.

Purpose: a pesquisa computou que `--focus-ring` (alpha .5) FALHA 3:1 em todos os fundos
do design (1.78 a 2.12) e que `--color-text-faint` (alpha .45) falha 4.5:1 por margem
minima (4.46). Ambos vieram da Fase 1 e sao bloqueadores de A11Y-03/A11Y-06. Este plano
tambem estabelece os contratos globais de scroll (SITE-05/ANIM-08), de reveal inerte
(ANIM-02) e de reducao de movimento (A11Y-07) que todas as secoes herdam.
Output: `tokens.css` corrigido e `base.css` com os contratos globais.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-static-zero-js-sections-csp-safe-refactor-a11y/03-RESEARCH.md
@.planning/phases/03-static-zero-js-sections-csp-safe-refactor-a11y/03-UI-SPEC.md
@.planning/phases/03-static-zero-js-sections-csp-safe-refactor-a11y/03-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar o verificador de contraste WCAG (scaffold Wave 0)</name>
  <files>(script no diretorio scratchpad da sessao — nao versionado)</files>
  <read_first>
    - .planning/phases/03-static-zero-js-sections-csp-safe-refactor-a11y/03-RESEARCH.md secao "Code Examples / Contrast-solver script" (funcoes hexToRgb, relLuminance, contrastRatio, compositeOver, minAlphaForRatio — usar exatamente estas, com limiar 0.04045 e nao 0.03928)
    - .planning/phases/03-static-zero-js-sections-csp-safe-refactor-a11y/03-RESEARCH.md secao "Common Pitfalls / Pitfall 2" e "Pitfall 3" (tabelas de valores computados que o script precisa reproduzir)
    - src/styles/tokens.css (valores atuais dos tokens a verificar)
  </read_first>
  <action>
    VALIDATION.md marca este item como lacuna de Wave 0: nao existe gate de contraste
    no CI deste repositorio. Crie o verificador ANTES de editar qualquer cor, para que
    a Task 2 tenha verificacao automatizada real em vez de julgamento visual.

    Grave o script como `contrast-check.mjs` no diretorio scratchpad da sessao. NAO
    commitar: D-13 dispensa explicitamente ferramenta de aprovacao versionada.

    O script implementa hexToRgb, relLuminance (limiar 0.04045), contrastRatio,
    compositeOver (source-over em sRGB) e minAlphaForRatio (busca binaria, 40
    iteracoes). Ele le `src/styles/tokens.css` por caminho relativo a raiz do repo,
    extrai por regex os valores declarados e roda estas asseveracoes:

    1. `--color-text-strong`, `--color-text`, `--color-text-muted` e
       `--color-text-faint` (branco com alpha) compostos sobre `#0A0A12`, `#05050A` e
       `#0D1B2A` atingem cada um >= 4.5.
    2. A cor declarada dentro de `--focus-ring`, composta sobre `#0A0A12`, `#FFFFFF`
       e `#F2F3F6`, atinge >= 3.0 em cada um. Se o valor do token for
       `var(--color-accent)`, resolva para `#6C4CFF` antes de medir.
    3. `--color-accent` opaco atinge >= 3.0 contra os tres fundos escuros (limite de
       texto grande) e >= 4.5 contra `#F2F3F6`.
    4. `--color-accent-light` atinge >= 4.5 contra os tres fundos escuros (e o
       substituto de D-14 para eyebrow e label de texto pequeno).
    5. `--color-light-text-muted` atinge >= 4.5 contra `#F2F3F6` e `#FFFFFF`.

    O script imprime uma linha OK ou FAIL por asseveracao e sai com codigo 1 se
    qualquer uma falhar. Rodado contra o `tokens.css` ATUAL (antes da Task 2) ele DEVE
    falhar em pelo menos duas asseveracoes, nomeadamente `--color-text-faint` e
    `--focus-ring`. Essa falha inicial e a prova de que o verificador mede de verdade;
    cole a saida vermelha no SUMMARY antes de seguir para a Task 2.
  </action>
  <verify>
    <automated>node "$SCRATCH/contrast-check.mjs" ; test $? -eq 1 && echo RED-CONFIRMADO</automated>
  </verify>
  <acceptance_criteria>
    - O arquivo `contrast-check.mjs` existe no scratchpad e roda sem erro de sintaxe
    - Rodado contra o `tokens.css` nao modificado, sai com codigo 1 e imprime FAIL nominalmente para `--color-text-faint` e para `--focus-ring`
    - A funcao de linearizacao usa o limiar `0.04045`
    - `git status --porcelain` nao lista nenhum arquivo de script novo dentro do repositorio
  </acceptance_criteria>
  <done>Existe um verificador de contraste executavel que comprova estar vermelho contra os tokens atuais.</done>
</task>

<task type="auto">
  <name>Task 2: Aplicar as correcoes de contraste e adicionar --nav-height em tokens.css</name>
  <files>src/styles/tokens.css</files>
  <read_first>
    - src/styles/tokens.css (arquivo inteiro, 112 linhas — os valores atuais de `--color-text-faint` na linha 25 e `--focus-ring` na linha 96)
    - .planning/phases/03-static-zero-js-sections-csp-safe-refactor-a11y/03-UI-SPEC.md secao "Contrast remediation - exact values to apply" (tabela travada; sao estes valores, nao recalcular a mao)
    - .planning/phases/03-static-zero-js-sections-csp-safe-refactor-a11y/03-CONTEXT.md D-13 e D-14
  </read_first>
  <action>
    Tres edicoes pontuais em `src/styles/tokens.css`, exatamente estas e mais nenhuma:

    1. `--color-text-faint`: de `rgba(255, 255, 255, 0.45)` para
       `rgba(255, 255, 255, 0.47)`. Motivo: 4.46 a 4.49 contra os tres fundos
       escuros, falha 4.5:1 por margem minima. Elevar uniformemente evita auditar
       tamanho de fonte caso a caso e nao produz diferenca visual perceptivel.

    2. `--focus-ring`: de `0 0 0 3px rgba(108, 76, 255, 0.5)` para
       `0 0 0 3px var(--color-accent)`. O anel a 50% de alpha falha 3:1 em TODOS os
       fundos do design. Opaco passa em todos: 3.86 no escuro, 5.10 no branco, 4.60
       no cinza claro. NAO troque por `--color-accent-light` aqui — ele falha 3:1
       contra fundos claros (2.6 a 2.9); aquele token e a correcao de TEXTO pequeno
       (D-14), problema diferente.

    3. Adicionar o token novo `--nav-height: 84px` logo apos `--space-gutter`, dentro
       do mesmo bloco de espacamento. Este valor e PROVISORIO: o plano 03-08 mede a
       altura real renderizada do SiteHeader em mobile e desktop e substitui o
       numero. Nao e aceitavel deixar o palpite sem revisao (Open Question 3 do
       RESEARCH).

    NAO altere `--color-accent`, `--color-accent-hover`, `--color-accent-light`,
    `--color-text-strong`, `--color-text` nem `--color-text-muted`: todos ja passam, e
    D-14 e explicito em corrigir o USO que falha, nunca o token base da marca.

    NAO altere `--color-light-text-faint` (#767C8E) neste plano. A UI-SPEC permite
    duas saidas para ele; a escolhida e trocar o USO especifico que falha (o helper
    "Resposta no mesmo dia util" do formulario) por `var(--color-light-text-muted)`,
    o que acontece no plano 03-07 dentro do componente. Manter o token intacto
    preserva a mesma filosofia de D-14.

    Zero comentarios no arquivo (regra de projeto vigente desde a Fase 1).
  </action>
  <verify>
    <automated>node "$SCRATCH/contrast-check.mjs" && grep -q -- "--nav-height: 84px" src/styles/tokens.css && grep -q -- "--focus-ring: 0 0 0 3px var(--color-accent)" src/styles/tokens.css && grep -q -- "--color-text-faint: rgba(255, 255, 255, 0.47)" src/styles/tokens.css</automated>
  </verify>
  <acceptance_criteria>
    - O verificador da Task 1 agora sai com codigo 0 e imprime OK para todas as asseveracoes
    - `grep -- "--color-text-faint" src/styles/tokens.css` retorna `rgba(255, 255, 255, 0.47)`
    - `grep -- "--focus-ring:" src/styles/tokens.css` retorna `0 0 0 3px var(--color-accent)`
    - `grep -- "--nav-height" src/styles/tokens.css` retorna `84px`
    - `git diff --stat src/styles/tokens.css` mostra no maximo 3 linhas adicionadas e 2 removidas; nenhuma linha de `--color-accent`, `--color-text-strong`, `--color-text`, `--color-text-muted` ou `--color-light-text-faint` aparece no diff
    - `grep -c "/\*" src/styles/tokens.css` retorna 0
  </acceptance_criteria>
  <done>Os dois tokens que falhavam WCAG passam pelo verificador automatizado, `--nav-height` existe como provisorio, e nenhum token de marca que ja passava foi tocado.</done>
</task>

<task type="auto">
  <name>Task 3: Escrever os contratos globais em base.css (foco, scroll, reveal, keyframes)</name>
  <files>src/styles/base.css</files>
  <read_first>
    - src/styles/base.css (arquivo inteiro, 19 linhas: reset de box-sizing, bloco body, reset de margem de h1 e p)
    - src/styles/tokens.css (tokens ja corrigidos pela Task 2 que este arquivo consome)
    - .planning/phases/03-static-zero-js-sections-csp-safe-refactor-a11y/03-UI-SPEC.md secoes "Reveal Markup Contract", "Anchor Navigation and Reduced-Motion Contract" e "Interaction States Contract"
    - arquivos de design/Dmarques Landing.dc.html linhas 20 a 30 (definicao original das keyframes dmFloat e dmPulse — referencia de intencao visual apenas, nunca copiar atributo style)
  </read_first>
  <action>
    Estenda `src/styles/base.css` preservando o que ja existe (reset de box-sizing,
    bloco body, reset de margem). Acrescente cinco blocos:

    1. Reset de margem ampliado: incluir `h2`, `h3`, `h4`, `figure`, `blockquote` na
       mesma regra que hoje zera `h1, p`. Acrescentar
       `img, svg, picture { display: block; max-width: 100% }` para eliminar o gap de
       baseline e evitar overflow horizontal.

    2. Anel de foco global (A11Y-03): regra `:focus-visible` com
       `outline: 3px solid var(--color-accent)` e
       `outline-offset: var(--focus-ring-offset)`. Use `outline` como padrao global
       em vez do box-shadow de `--focus-ring` porque `outline` nunca e recortado por
       ancestral com `overflow: hidden`, situacao real no hero e no CTA final. O token
       `--focus-ring` continua disponivel para componentes que precisem do anel em
       box-shadow, notadamente os inputs do formulario. Os dois usam
       `--color-accent` opaco, entao ambos passam 3:1. Acrescente tambem
       `:focus:not(:focus-visible) { outline: none }` para nao exibir anel em clique
       de mouse.

    3. Scroll de ancora (SITE-05 e ANIM-08): `html { scroll-behavior: smooth }`;
       `:is(#servicos, #processo, #diferenciais, #sobre, #contato, #faq) { scroll-margin-top: var(--nav-height) }`
       — inclua os seis ids de secao que o design source define, nao apenas os quatro
       citados em SITE-05, porque nav e links internos podem apontar para qualquer um;
       e `@media (prefers-reduced-motion: reduce) { html { scroll-behavior: auto } }`.

    4. Contrato de reveal inerte (ANIM-02): reproduza o bloco da UI-SPEC secao
       "Reveal Markup Contract" sem desviar. Estado padrao
       `[data-reveal] { opacity: 1; transform: none }`; dentro de
       `@media (prefers-reduced-motion: no-preference)`, a regra
       `html.js-ready [data-reveal]` com opacity 0, `translateY(20px)` e transition em
       `var(--dur-reveal) var(--ease-out)` sobre opacity e transform, e a regra
       `html.js-ready [data-reveal].is-revealed` devolvendo opacity 1 e
       `transform: none`. Os nomes `js-ready` e `is-revealed` sao contrato fixo: a
       Fase 4 se conecta exatamente a eles. Como a Fase 3 nao embarca JS algum,
       nenhuma classe `js-ready` chega a existir e todo elemento renderiza visivel por
       construcao.

    5. Keyframes decorativas e guarda de movimento: declare `@keyframes dmFloat`
       (translateY de 0 para -14px e de volta a 0) e `@keyframes dmPulse` (opacity de
       .35 para .75 e de volta a .35), com os mesmos nomes do design source, para que
       os componentes possam referencia-las. Em seguida um bloco
       `@media (prefers-reduced-motion: reduce)` aplicado a `*, *::before, *::after`
       com `animation-duration: 0.01ms !important`,
       `animation-iteration-count: 1 !important` e
       `transition-duration: 0.01ms !important`. Isso cobre A11Y-07 estruturalmente:
       nenhum componente posterior precisa lembrar de escrever a propria guarda, e o
       caminho reduzido continua parecendo acabado porque o estado final de cada
       elemento ja e o estado visivel.

    Regra transversal que este arquivo estabelece e todos os componentes seguem
    (ANIM-07): transicoes somente em `transform`, `opacity`, `box-shadow` e
    `border-color`, duracao maxima `var(--dur-slow)` (300ms). Nao introduza aqui
    nenhuma transicao em propriedade que dispara layout.

    Zero comentarios no arquivo.
  </action>
  <verify>
    <automated>pnpm build && node -e "const c=require('fs').readFileSync('src/styles/base.css','utf8');const n=['scroll-behavior: smooth','scroll-margin-top: var(--nav-height)','prefers-reduced-motion: reduce','prefers-reduced-motion: no-preference','html.js-ready','is-revealed','dmFloat','dmPulse',':focus-visible','outline-offset: var(--focus-ring-offset)'];const m=n.filter(x=>!c.includes(x));if(m.length){console.error('FALTANDO',m);process.exit(1)};if(c.includes('/*')){console.error('COMENTARIO');process.exit(1)};console.log('base.css OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm build` sai com codigo 0
    - `src/styles/base.css` contem literalmente `scroll-behavior: smooth`, `scroll-margin-top: var(--nav-height)`, `html.js-ready`, `is-revealed`, `dmFloat`, `dmPulse`, `:focus-visible` e `outline-offset: var(--focus-ring-offset)`
    - Existem exatamente dois blocos `@media (prefers-reduced-motion: reduce)` ou um bloco consolidado, e um bloco `@media (prefers-reduced-motion: no-preference)`
    - `grep -c "/\*" src/styles/base.css` retorna 0
    - `grep -nE "transition[^;]*(width|height|top|left|margin|padding)" src/styles/base.css` nao retorna nada
    - `bash scripts/security-check.sh --ci` nao regride o numero de PASS em relacao ao estado anterior ao plano
  </acceptance_criteria>
  <done>base.css estabelece foco visivel com contraste, scroll de ancora com offset, reveal inerte visivel-por-padrao e reducao de movimento global, tudo verificado por build e por asseveracao de conteudo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS global -> superficie de estilo da CSP da Fase 7 | o que este arquivo permite ou proibe define o `style-src` que a Fase 7 podera travar |
| preferencia do sistema operacional (prefers-reduced-motion) -> render | entrada do usuario que altera comportamento visual |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | `src/styles/base.css`, `src/styles/tokens.css` | mitigate | Todo estilo entra como CSS externo em arquivo `.css`, nunca como atributo `style=` nem `<style>` inline; `build.inlineStylesheets: 'never'` ja garante saida externa. Verificado por `scripts/security-check.sh` verificacao 2 e verificacao 3. |
| T-03-06 | Denial of Service | guarda global de reduced-motion | mitigate | A regra `*` de reducao de movimento evita animacao infinita em usuarios sensiveis a movimento (WCAG 2.3.3) e limita custo de composicao; o estado final de todo elemento permanece visivel, entao nenhum conteudo fica inacessivel se a animacao for suprimida. |
| T-03-07 | Repudiation | correcao de contraste sem trilha | mitigate | D-13 dispensa planilha de aprovacao, mas a Task 1 obriga um verificador executavel cuja saida vermelha e depois verde e colada no SUMMARY, criando evidencia reproduzivel de que os valores foram medidos e nao estimados. |
| T-03-02 | Tampering | tokens de marca (`--color-accent*`) | accept | Nao sao alterados neste plano; o risco de regressao visual por mudanca de token base fica eliminado por construcao (criterio de aceite proibe que apareçam no diff). |
</threat_model>

<verification>
- `pnpm build` sai com codigo 0.
- `bash scripts/security-check.sh --ci` mantem ou melhora a contagem de PASS anterior.
- O verificador de contraste do scratchpad sai com codigo 0 contra o `tokens.css` final.
</verification>

<success_criteria>
- `--focus-ring` e `--color-text-faint` passam os limiares WCAG medidos programaticamente.
- `--nav-height` existe como token (valor provisorio, revisado em 03-08).
- `base.css` declara foco visivel, scroll com offset e reversao sob reduced-motion, reveal inerte e keyframes com guarda global.
- Nenhum arquivo de `src/` contem comentario ou atributo `style=`.
</success_criteria>

<output>
Create `.planning/phases/03-static-zero-js-sections-csp-safe-refactor-a11y/03-02-SUMMARY.md` when done
</output>
