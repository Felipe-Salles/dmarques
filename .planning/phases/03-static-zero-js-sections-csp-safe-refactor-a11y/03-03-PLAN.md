---
phase: 03-static-zero-js-sections-csp-safe-refactor-a11y
plan: 03
type: execute
wave: 2
depends_on: ["03-02"]
files_modified:
  - src/components/SiteHeader.astro
  - src/pages/obrigado.astro
  - src/pages/politica-de-privacidade.astro
  - src/pages/404.astro
autonomous: true
requirements: [SITE-06, SITE-07, SITE-08, A11Y-01, A11Y-02, A11Y-04, A11Y-07, ANIM-07, SEO-02]

must_haves:
  truths:
    - "O primeiro elemento focável da home é um skip link visível que leva a #conteudo"
    - "D-04: Em viewport estreita o menu em <details>/<summary> abre e fecha por teclado (Enter/Space) sem JavaScript e sem prender o foco"
    - "D-05: O CTA 'Pedir orçamento' continua visível no mobile, fora do <details>, sem precisar abrir o menu"
    - "As rotas /obrigado, /politica-de-privacidade e /404 respondem com página branded"
  artifacts:
    - path: "src/components/SiteHeader.astro"
      provides: "Skip link, logo, nav desktop, disclosure <details> mobile com gatilho hambúrguer + aria-label (D-06) e CTA pill"
      contains: "details"
    - path: "src/pages/obrigado.astro"
      provides: "Página de agradecimento pós-formulário (SITE-07)"
    - path: "src/pages/politica-de-privacidade.astro"
      provides: "Reserva da URL da política; corpo LGPD é Fase 5 (D-12)"
    - path: "src/pages/404.astro"
      provides: "Página 404 branded (SITE-08)"
  key_links:
    - from: "src/components/SiteHeader.astro"
      to: "src/pages/index.astro"
      via: "âncoras #servicos #processo #sobre #contato e href=#conteudo do skip link"
      pattern: "href=\"#conteudo\""
    - from: "src/pages/obrigado.astro"
      to: "src/layouts/BaseLayout.astro"
      via: "prop title do BaseLayout"
      pattern: "BaseLayout"
---

<objective>
Construir o cabeçalho do site (com skip link, nav por âncora e disclosure mobile nativo)
e as três rotas utilitárias do projeto.

Purpose: o cabeçalho é a peça com maior densidade de requisitos de acessibilidade da
fase (A11Y-01 skip link, A11Y-04 operável por teclado, SITE-06 nav mobile sem
framework, SITE-05 âncoras) e é consumido por `index.astro`; as três rotas utilitárias
não dependem de nenhuma seção e podem sair no mesmo plano sem conflito de arquivo.
Output: `SiteHeader.astro` + `obrigado.astro` + `politica-de-privacidade.astro` +
`404.astro`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-static-zero-js-sections-csp-safe-refactor-a11y/03-UI-SPEC.md
@.planning/phases/03-static-zero-js-sections-csp-safe-refactor-a11y/03-PATTERNS.md
@.planning/phases/03-static-zero-js-sections-csp-safe-refactor-a11y/03-RESEARCH.md
</context>

<interfaces>
Contratos que este plano CONSOME (já existentes no repositório):

- `src/layouts/BaseLayout.astro` aceita a prop `title` (default
  `'Dmarques — Soluções Web'`), já emite `<html lang="pt-BR">` (SEO-02) e injeta
  `tokens.css` + `base.css` + as duas `<Font>` + `<Analytics />`. Nenhuma alteração
  estrutural nele é necessária nesta fase.
- `src/styles/base.css` (plano 03-02) já fornece: `:focus-visible` global com
  `outline: 3px solid var(--color-accent)`, `scroll-margin-top: var(--nav-height)`
  nos seis ids de seção, `scroll-behavior` com reversão sob reduced-motion, o
  contrato `[data-reveal]` e a guarda global de reduced-motion.
- `src/styles/tokens.css` (plano 03-02) já fornece `--nav-height`, `--focus-ring`
  opaco e `--color-text-faint` corrigido.

Contratos que este plano PRODUZ (consumidos pelo plano 03-08):

- `SiteHeader.astro` sem props: renderiza o skip link como PRIMEIRO nó, depois
  `<header>`. Deve ser posicionado por `index.astro` imediatamente antes de
  `<main id="conteudo">`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: SiteHeader.astro — skip link, nav desktop, disclosure mobile e CTA</name>
  <files>src/components/SiteHeader.astro</files>
  <read_first>
    - src/pages/index.astro (único padrão `.astro` existente: frontmatter só com imports, `<style>` escopado 100% em `var(--token)`, zero comentário)
    - src/layouts/BaseLayout.astro (onde o componente será renderizado, via `<slot />`)
    - src/styles/tokens.css (tokens de cor, raio, blur, z-index e duração disponíveis)
    - src/styles/base.css (o `:focus-visible` global já aplicado — não duplicar, apenas sobrescrever onde o anel precisar de tratamento próprio)
    - arquivos de design/Dmarques Landing.dc.html linhas 148 a 165 (header do "03 Bleed": wordmark, quatro links, pill "Pedir orçamento" — referência VISUAL apenas)
    - .planning/phases/03-static-zero-js-sections-csp-safe-refactor-a11y/03-UI-SPEC.md seção "Mobile Navigation Contract"
  </read_first>
  <action>
    Crie `src/components/SiteHeader.astro`. Estrutura de markup, nesta ordem exata:

    1. `<a href="#conteudo" class="skip-link">Pular para o conteúdo</a>` como o
       primeiríssimo nó do componente (A11Y-01 exige que seja o primeiro elemento
       focável da página; o componente é renderizado antes de `<main>` por
       `index.astro`). CSS: posicionado fora da tela com `top: -100%`, trazido para
       `top: var(--space-4)` no estado `:focus-visible`, usando
       `z-index: var(--z-skiplink)`, fundo `var(--color-accent)`, texto
       `var(--color-text-on-accent)`, `border-radius: var(--radius-md)`, transição
       somente em `top` — e como `top` dispara layout, use aqui `transform:
       translateY()` em vez de animar `top`, mantendo ANIM-07 (transições apenas em
       transform/opacity/box-shadow/border-color).

    2. `<header>` como landmark (A11Y-02). Dentro dele, um container centralizado
       (`max-width: 1320px`, `margin-inline: auto`, `padding-inline:
       var(--space-gutter)`) com três grupos: wordmark, `<nav>` desktop, e o bloco
       mobile.

    3. Wordmark: um `<a href="/">` contendo o quadrado "D" (32x32,
       `border-radius: var(--radius-md)`, borda `var(--color-accent-border)`) e o
       texto "Dmarques" seguido de `<span>Soluções Web</span>` em
       `var(--color-text-faint)`. O quadrado "D" é texto real, não imagem.

    4. `<nav aria-label="Navegação principal">` desktop com os quatro links de âncora
       nesta ordem e com estes textos: "Serviços" para `#servicos`,
       "Como trabalhamos" para `#processo`, "Sobre" para `#sobre`, "Contato" para
       `#contato`. Cada link: `var(--color-text-muted)` no repouso e
       `var(--color-text-strong)` em `:hover, :focus-visible` na MESMA regra
       (ANIM-07), transição `color var(--dur-fast) var(--ease-standard)`.

    5. Disclosure mobile (D-04/D-06): `<details class="nav-toggle">` com
       `<summary aria-label="Abrir menu de navegação">` contendo um `<svg
       aria-hidden="true">` de hambúrguer de três traços (stroke `currentColor`,
       `stroke-width="1.6"`, `stroke-linecap="round"`, viewBox 0 0 24 24). Nenhum
       texto "Menu" visível. O `<summary>` precisa de área de toque mínima de
       44x44px (use padding, não width/height do `<svg>`). Remoção do marcador exige
       AS DUAS regras juntas: `list-style: none` no `summary` E
       `summary::-webkit-details-marker { display: none }` — uma sozinha não basta
       entre navegadores. Dentro do `<details>`, um segundo `<nav
       aria-label="Navegação principal (mobile)">` com os mesmos quatro links.
       NÃO adicione `aria-expanded` manualmente: o navegador gerencia isso nativamente
       no `<summary>`; adicionar à mão cria estado dessincronizado.

    6. Painel mobile (discrição travada na UI-SPEC): dropdown de largura total
       ancorado abaixo do header — `position: absolute; top: 100%; left: 0; right: 0`
       sobre o `<header>` com `position: relative`. Transição de abertura em
       `opacity` + `transform: translateY(-8px)` para `translateY(0)`, duração
       `var(--dur-mid)`, `var(--ease-out)`, envolvida em
       `@media (prefers-reduced-motion: no-preference)`.

    7. CTA pill "Pedir orçamento" apontando para `#contato`, renderizado como IRMÃO
       do `<details>` (D-05) — nunca dentro dele — para continuar visível no mobile.
       Estado `:hover, :focus-visible` na mesma regra: fundo e borda
       `var(--color-accent)`.

    Breakpoints: o `<nav>` desktop aparece a partir de 860px e o `.nav-toggle` fica
    `display: none` nesse ponto; abaixo disso inverte. Use uma única media query para
    a troca, não duas concorrentes.

    RESTRIÇÃO CRÍTICA DE LAYOUT (Pitfall 4): NÃO envolva o header em nenhum wrapper
    com `overflow: hidden` ou `overflow-x: clip`. O design source aplica
    `overflow-x: clip` num wrapper da página inteira (linha 28) — não reproduza isso.
    Esse recorte deve ficar escopado só aos containers que realmente precisam (imagem
    do hero, canvas, glow do CTA final), senão o painel mobile expandido é cortado
    mesmo com z-index correto.

    Sintaxe do design-editor PROIBIDA de transplantar (Pitfall 5): nenhum
    `style="..."`, nenhum `style-hover="..."`, nenhum `style-focus="..."`, nenhum
    `onClick="{{ ... }}"`, nenhum `ref="{{ ... }}"`, nenhum `<image-slot>`, nenhum
    `<sc-if>`. Cada `style-hover` do design vira uma regra `:hover, :focus-visible`
    real no `<style>` escopado.

    Todo valor de cor, raio, espaçamento, duração e z-index vem de `var(--token)`;
    nenhum hex literal no `<style>`. Zero comentários no arquivo.
  </action>
  <verify>
    <automated>pnpm check && bash scripts/security-check.sh --ci 2>&1 | grep -q "PASS: verificacao 2" && node -e "const c=require('fs').readFileSync('src/components/SiteHeader.astro','utf8');const need=['href=\"#conteudo\"','Pular para o conteúdo','aria-label=\"Abrir menu de navegação\"','list-style: none','::-webkit-details-marker','href=\"#servicos\"','href=\"#processo\"','href=\"#sobre\"','href=\"#contato\"','Pedir orçamento',':focus-visible'];const m=need.filter(x=>!c.includes(x));if(m.length){console.error('FALTANDO',m);process.exit(1)};const bad=['style-hover','style-focus','onClick','image-slot','sc-if','{{'];const b=bad.filter(x=>c.includes(x));if(b.length){console.error('SINTAXE DE EDITOR VAZOU',b);process.exit(1)};if(/#[0-9a-fA-F]{6}/.test(c)){console.error('HEX LITERAL');process.exit(1)};if(c.includes('/*')||/^\s*\/\//m.test(c)){console.error('COMENTARIO');process.exit(1)};console.log('SiteHeader OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm check` (astro sync + astro check) sai com código 0
    - `bash scripts/security-check.sh --ci` imprime `PASS: verificacao 2 - nenhum atributo style= em src/`
    - O arquivo contém `href="#conteudo"`, o texto `Pular para o conteúdo`, `aria-label="Abrir menu de navegação"`, os quatro hrefs de âncora e o texto `Pedir orçamento`
    - O bloco `<style>` contém AS DUAS regras de remoção de marcador: `list-style: none` no seletor de `summary` e `::-webkit-details-marker`
    - `grep -c 'aria-expanded' src/components/SiteHeader.astro` retorna 0
    - O elemento `<a class="cta-pill">` NÃO é descendente do `<details>` no markup
    - Nenhuma ocorrência de `style-hover`, `style-focus`, `onClick`, `image-slot`, `sc-if` ou `{{` no arquivo
    - Nenhum hex literal de 6 dígitos e nenhum comentário no arquivo
    - Toda regra `:hover` do `<style>` tem um `:focus-visible` no mesmo seletor agrupado (confira manualmente linha a linha e registre a contagem no SUMMARY)
  </acceptance_criteria>
  <done>SiteHeader.astro existe com skip link em primeiro lugar, nav por âncora, disclosure `<details>` nativo com CTA fora dele, zero JS, zero inline style e zero sintaxe de editor.</done>
</task>

<task type="auto">
  <name>Task 2: As três rotas utilitárias (/obrigado, /politica-de-privacidade, /404)</name>
  <files>src/pages/obrigado.astro, src/pages/politica-de-privacidade.astro, src/pages/404.astro</files>
  <read_first>
    - src/pages/index.astro (padrão de página: import do BaseLayout, `<main id="conteudo">`, `<style>` escopado)
    - src/layouts/BaseLayout.astro (prop `title` e o que já vem de graça: lang pt-BR, fontes, analytics, CSS)
    - .planning/phases/03-static-zero-js-sections-csp-safe-refactor-a11y/03-UI-SPEC.md seção "Copywriting Contract" e "New Routes Contract" (copy verbatim das três rotas)
    - .planning/phases/03-static-zero-js-sections-csp-safe-refactor-a11y/03-CONTEXT.md D-12 (por que a política é só casca nesta fase)
  </read_first>
  <action>
    Três páginas, cada uma importando `BaseLayout` e passando `title`, cada uma com um
    único `<main id="conteudo">` (mesmo id do skip link — nunca inventar outro), um
    único `<h1>` e o corpo verbatim da UI-SPEC. As três NÃO renderizam `SiteHeader`
    nem rodapé completo: são rotas utilitárias, e o header completo pertence só à home
    (padrão registrado em 03-PATTERNS.md).

    `src/pages/obrigado.astro` — title `"Pedido enviado — Dmarques"`;
    H1 `"Recebemos seu pedido"`; parágrafo
    `"Respondemos no mesmo dia útil. Enquanto isso, fale com a gente no WhatsApp se preferir."`;
    link `"Voltar para a home"` para `/`.

    `src/pages/politica-de-privacidade.astro` — title
    `"Política de Privacidade — Dmarques"`; H1 `"Política de Privacidade"`; parágrafo
    `"Esta página será publicada com o conteúdo completo no lançamento do formulário."`.
    Só isso: D-12 reserva a URL, o corpo LGPD (LGPD-01/02) é da Fase 5 e precisa sair
    no mesmo release do formulário ativo. NÃO escreva controlador, bases legais,
    retenção nem direitos do titular aqui — publicar política incompleta é pior que
    não publicar.

    `src/pages/404.astro` — title `"Página não encontrada — Dmarques"`; H1
    `"Página não encontrada"`; parágrafo
    `"O endereço que você tentou acessar não existe."`; link `"Voltar para a home"`
    para `/`. A página NÃO pode expor caminho requisitado, stack trace, versão de
    framework nem qualquer diagnóstico (mitigação de T-03-03; SEC-04 formaliza isso
    na Fase 7, mas a página nasce aqui).

    Nenhuma das três precisa de `export const prerender` — `output: 'static'`
    prerenderiza tudo por padrão; só o futuro `/api/orcamento` (Fase 5) vai precisar
    de `prerender = false`.

    Estilo: um `<style>` escopado por página, compartilhando o mesmo tratamento
    visual — fundo herdado de `body`, `<main>` centralizado com
    `min-height: 100svh`, `padding: var(--space-16) var(--space-gutter)`, H1 em
    `var(--font-heading)` peso `var(--font-weight-bold)`, cor
    `var(--color-text-strong)`, `text-wrap: balance`; parágrafo em
    `var(--color-text-muted)`, `var(--leading-body)`, `text-wrap: pretty`; o link de
    volta com `:hover, :focus-visible` agrupados mudando a cor para
    `var(--color-accent-light)` (não `--color-accent`: como texto pequeno em fundo
    escuro, `#6C4CFF` falha 4.5:1 — D-14). Só `var(--token)`, zero hex literal, zero
    comentário.
  </action>
  <verify>
    <automated>pnpm build && node -e "const fs=require('fs');const map={'.vercel/output/static/obrigado/index.html':['Recebemos seu pedido','Voltar para a home'],'.vercel/output/static/politica-de-privacidade/index.html':['Política de Privacidade'],'.vercel/output/static/404.html':['Página não encontrada','Voltar para a home']};for(const [f,needles] of Object.entries(map)){if(!fs.existsSync(f)){console.error('AUSENTE',f);process.exit(1)};const h=fs.readFileSync(f,'utf8');for(const n of needles){if(!h.includes(n)){console.error('SEM COPY',f,n);process.exit(1)}};if(!h.includes('lang=\"pt-BR\"')){console.error('SEM lang pt-BR',f);process.exit(1)};if((h.match(/<h1/g)||[]).length!==1){console.error('H1 != 1',f);process.exit(1)};if(!h.includes('id=\"conteudo\"')){console.error('SEM main id conteudo',f);process.exit(1)}};console.log('rotas OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm build` sai com código 0
    - Existem `.vercel/output/static/obrigado/index.html`, `.vercel/output/static/politica-de-privacidade/index.html` e `.vercel/output/static/404.html`
    - Cada um dos três HTML contém `lang="pt-BR"`, exatamente um `<h1>` e `id="conteudo"`
    - O HTML de `/obrigado` contém a frase `Respondemos no mesmo dia útil.`
    - O HTML de `/politica-de-privacidade` NÃO contém as palavras `controlador`, `ANPD`, `retenção` nem `Art.` (a casca não antecipa conteúdo da Fase 5)
    - O HTML de `/404` não contém `stack`, `Error:`, `astro@` nem o caminho requisitado
    - `bash scripts/security-check.sh --ci` mantém `PASS: verificacao 2`
    - Nenhum dos três arquivos `.astro` contém comentário ou hex literal
  </acceptance_criteria>
  <done>As três rotas utilitárias existem, buildam para HTML estático com landmark e H1 corretos, copy verbatim da UI-SPEC e nenhum vazamento de diagnóstico.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador do visitante -> HTML estático | única superfície desta fase; nenhum input do usuário é processado |
| caminho de URL inexistente -> página 404 | entrada não confiável controlada pelo visitante chega ao roteamento |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Information Disclosure | `src/pages/404.astro` | mitigate | Página 404 é HTML estático com copy fixa; proibido ecoar o caminho requisitado, stack trace ou versão de framework. Critério de aceite grepa por `stack`, `Error:`, `astro@` no HTML construído. SEC-04 formaliza na Fase 7. |
| T-03-08 | Information Disclosure | `src/pages/politica-de-privacidade.astro` | mitigate | Publicar política LGPD incompleta ou incorreta cria exposição regulatória. A casca declara explicitamente que o conteúdo sai no lançamento do formulário (D-12/LGPD-01) e o critério de aceite proíbe termos legais antecipados. |
| T-03-01 | Tampering | `SiteHeader.astro` | mitigate | Zero `style=`, zero handler inline, zero `<script>`: o header não amplia a superfície que o `script-src`/`style-src` estrito da Fase 7 terá de permitir. Verificado por `security-check.sh` verificacao 2 e por grep de sintaxe de editor (`onClick`, `{{`). |
| T-03-09 | Denial of Service (usabilidade) | disclosure `<details>` mobile | accept | Sem JS não há fechamento por clique fora do painel (limitação nativa do `<details>`, aceita em D-04). Não viola SITE-06, que exige apenas operabilidade por teclado e ausência de armadilha de foco. Reavaliar como enhancement na Fase 4 se virar reclamação real. |
</threat_model>

<verification>
- `pnpm check` e `pnpm build` saem com código 0.
- `bash scripts/security-check.sh --ci` mantém `PASS: verificacao 2`.
- `grep -rn 'onClick\|onSubmit\|{{' src/` não retorna nada.
</verification>

<success_criteria>
- `SiteHeader.astro` entrega skip link como primeiro nó, nav por âncora com par `:hover`/`:focus-visible`, disclosure `<details>` com remoção de marcador cross-browser e CTA fora do menu.
- As três rotas utilitárias buildam para HTML estático com `lang="pt-BR"`, um `<h1>` e `<main id="conteudo">`.
- Nenhum arquivo novo contém `style=`, hex literal, comentário ou sintaxe do design-editor.
</success_criteria>

<output>
Create `.planning/phases/03-static-zero-js-sections-csp-safe-refactor-a11y/03-03-SUMMARY.md` when done
</output>
