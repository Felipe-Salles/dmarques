---
phase: 03-static-zero-js-sections-csp-safe-refactor-a11y
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-lock.yaml
  - src/assets/hero-render-placeholder.webp
  - src/assets/founder-portrait-placeholder.webp
autonomous: true
requirements: [SITE-09, PERF-05]

must_haves:
  truths:
    - "`pnpm build` consegue processar um `<Image>`/`<Picture>` de asset local sem lançar MissingSharp"
    - "D-01, D-03: Existe uma imagem placeholder branded do render 3D do hero na proporção 4:3 com sangria à direita reservada"
    - "D-01: Existe uma imagem placeholder branded do retrato do fundador na proporção 4:5"
    - "Nenhuma otimização de imagem em runtime é introduzida (Sharp só em build)"
  artifacts:
    - path: "package.json"
      provides: "devDependency sharp pinada em 0.35.4"
      contains: "\"sharp\": \"0.35.4\""
    - path: "src/assets/hero-render-placeholder.webp"
      provides: "Fonte do render 3D do hero (1920x1440, <300 KB)"
    - path: "src/assets/founder-portrait-placeholder.webp"
      provides: "Fonte do retrato do fundador (960x1200, <300 KB)"
  key_links:
    - from: "package.json"
      to: "node_modules/sharp"
      via: "devDependency explícita exigida pelo layout estrito do pnpm"
      pattern: "\"sharp\":\\s*\"0\\.35\\.4\""
---

<objective>
Destravar o pipeline de imagens de build (`astro:assets` + Sharp) e produzir os dois
assets placeholder que o Hero e a seção Sobre vão consumir.

Purpose: sob o layout estrito do pnpm, `<Image>`/`<Picture>` falham com `MissingSharp`
porque `sharp` só existe como dependência transitiva de `astro`. Sem esta correção,
todo build de plano posterior que renderize imagem quebra por um motivo que parece
erro de autoria. Os placeholders (D-01) impedem que a fase fique bloqueada esperando
o render 3D e a foto reais.
Output: `sharp@0.35.4` como devDependency explícita + dois `.webp` em `src/assets/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-static-zero-js-sections-csp-safe-refactor-a11y/03-RESEARCH.md
@.planning/phases/03-static-zero-js-sections-csp-safe-refactor-a11y/03-UI-SPEC.md
@.planning/phases/03-static-zero-js-sections-csp-safe-refactor-a11y/03-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Promover sharp a devDependency explícita pinada</name>
  <files>package.json, pnpm-lock.yaml</files>
  <read_first>
    - package.json (ver o estilo de pin exato já usado: `astro` 7.3.1, `@astrojs/vercel` 11.0.10, sem `^`/`~` salvo `@astrojs/check`)
    - pnpm-workspace.yaml (conferir `onlyBuiltDependencies`; a pesquisa confirma que sharp NÃO precisa de entrada nova)
    - .planning/phases/03-static-zero-js-sections-csp-safe-refactor-a11y/03-RESEARCH.md § "Common Pitfalls · Pitfall 1" e § "Package Legitimacy Audit"
  </read_first>
  <action>
    Antes de instalar, rode `pnpm why sharp` e confirme que a versão já resolvida
    transitivamente por `astro@7.3.1` é exatamente `0.35.4`. Se for outra versão,
    PARE e reporte — instalar uma versão diferente introduziria uma segunda cópia de
    sharp na árvore, o que a auditoria de legitimidade do RESEARCH não cobre.

    Confirmando 0.35.4, execute `pnpm add -D sharp@0.35.4`. O resultado esperado em
    `package.json` é a chave `"sharp": "0.35.4"` dentro de `devDependencies`, em ordem
    alfabética entre `prettier-plugin-astro` e `typescript`, sem prefixo `^` ou `~`
    (mesmo padrão de pin exato das demais dependências diretas do projeto).

    NÃO adicione `sharp` a `onlyBuiltDependencies` em `pnpm-workspace.yaml` — sharp
    distribui binários pré-compilados como optionalDependencies e não roda script de
    postinstall bloqueado pelo pnpm (verificado empiricamente na pesquisa, A4).

    NÃO altere `astro.config.mjs`: `imageService: true` no adapter da Vercel é
    proibido por PERF-05, e `passthroughImageService()` violaria SITE-09. A correção
    é exclusivamente de declaração de dependência.

    Rode `pnpm audit --audit-level=high` depois da instalação e confirme saída zero —
    a árvore resolvida não muda (mesma versão já auditada limpa), então qualquer
    achado novo é sinal de que a versão instalada divergiu.
  </action>
  <verify>
    <automated>cd "F:/Projetos/dmarques" && grep -q '"sharp": "0.35.4"' package.json && pnpm why sharp | grep -c '0.35.4' && pnpm audit --audit-level=high && node -e "import('sharp').then(s=>{if(!s.default.versions.sharp)process.exit(1);console.log('sharp',s.default.versions.sharp)})"</automated>
  </verify>
  <acceptance_criteria>
    - `grep '"sharp"' package.json` retorna exatamente `"sharp": "0.35.4"` e a chave está sob `devDependencies`, não `dependencies`
    - `pnpm why sharp` não lista nenhuma versão de sharp diferente de `0.35.4`
    - `node -e "import('sharp').then(s=>console.log(s.default.versions.sharp))"` imprime uma versão sem lançar erro de resolução
    - `pnpm audit --audit-level=high` sai com código 0
    - `git diff --name-only` inclui apenas `package.json` e `pnpm-lock.yaml`; `astro.config.mjs` e `pnpm-workspace.yaml` estão intocados
  </acceptance_criteria>
  <done>`sharp@0.35.4` é devDependency explícita, resolvível por import bare, auditoria limpa, e nenhum arquivo de configuração de build foi tocado.</done>
</task>

<task type="auto">
  <name>Task 2: Gerar os dois assets placeholder branded via Sharp</name>
  <files>src/assets/hero-render-placeholder.webp, src/assets/founder-portrait-placeholder.webp</files>
  <read_first>
    - src/content/cases/dmarques-cover.webp (precedente de placeholder da Fase 2 — inspecione dimensões e peso com `node -e` + sharp metadata para calibrar a disciplina de tamanho)
    - src/styles/tokens.css (paleta oficial: `--color-bg` #0a0a12, `--color-bg-deep` #05050a, `--color-bg-blue` #0d1b2a, `--color-accent` #6c4cff, `--color-accent-light` #9a85ff)
    - .planning/phases/03-static-zero-js-sections-csp-safe-refactor-a11y/03-UI-SPEC.md § "Image Placeholder Contract"
    - .planning/phases/02-content-collections/02-CONTEXT.md § D-08 (regra "somente formas vetoriais, sem elemento `<text>`")
  </read_first>
  <action>
    Escreva um script Node one-off NO DIRETÓRIO SCRATCHPAD da sessão (não versionado,
    não commitado — mesmo precedente da Fase 2, em que o script gerador de
    `dmarques-cover.webp` foi descartado após uso). O script importa `sharp`
    diretamente (agora resolvível após a Task 1 — não use mais o truque
    `createRequire` da Fase 2), compõe cada imagem a partir de um buffer SVG e
    grava `.webp`.

    Arte-direção obrigatória para ambas: SOMENTE formas vetoriais (retângulos,
    círculos, elipses, paths, gradientes lineares/radiais). ZERO elemento `<text>`
    no SVG — texto rasterizado dentro da imagem nunca é acessível nem traduzível e
    quebraria A11Y-05 na troca pelo asset real.

    Asset 1 — `src/assets/hero-render-placeholder.webp`
    - Dimensões exatas: 1920 x 1440 (proporção 4:3, o dobro do `width={960}
      height={720}` que `HeroBleed.astro` vai declarar; evita upscale).
    - Tema: composição abstrata de "render 3D neon roxo" — fundo `#0a0a12`, formas
      geométricas sobrepostas em `#6c4cff` e `#9a85ff` com gradientes e um halo
      radial. Nenhuma borda arredondada assada na imagem: o
      `border-radius: 24px 0 0 24px` e a sangria à direita (D-03) são CSS no
      componente, não parte do raster.
    - Alvo de peso: < 300 KB (qualidade webp ~80; reduza a qualidade se estourar).

    Asset 2 — `src/assets/founder-portrait-placeholder.webp`
    - Dimensões exatas: 960 x 1200 (proporção 4:5, o dobro do `width={480}
      height={600}` de `AboutSection.astro`).
    - Tema: silhueta/retrato abstrato branded — fundo com gradiente de `#0d1b2a`
      para `#0a0a12`, silhueta de ombros/cabeça em tom neutro sobre halo `#6c4cff`.
      Novamente, zero `<text>`; o `border-radius: 20px` é CSS.
    - Alvo de peso: < 300 KB.

    Crie o diretório `src/assets/` (ainda não existe no repositório). Depois de
    gravar os dois arquivos, confirme com sharp metadata que largura/altura e
    formato batem com o especificado e que o tamanho em disco está abaixo do teto.

    Não commite o script gerador. Não adicione comentário algum a arquivo versionado
    (regra de projeto vigente desde a Fase 1).
  </action>
  <verify>
    <automated>cd "F:/Projetos/dmarques" && node -e "const s=require('sharp');const fs=require('fs');(async()=>{const specs=[['src/assets/hero-render-placeholder.webp',1920,1440],['src/assets/founder-portrait-placeholder.webp',960,1200]];for(const [p,w,h] of specs){const m=await s(p).metadata();const b=fs.statSync(p).size;if(m.format!=='webp'||m.width!==w||m.height!==h||b>300000){console.error('FAIL',p,m.format,m.width,m.height,b);process.exit(1)}console.log('OK',p,m.width+'x'+m.height,b+'B')}})()"</automated>
  </verify>
  <acceptance_criteria>
    - `src/assets/hero-render-placeholder.webp` existe, formato `webp`, exatamente 1920x1440, menos de 300000 bytes
    - `src/assets/founder-portrait-placeholder.webp` existe, formato `webp`, exatamente 960x1200, menos de 300000 bytes
    - O SVG de origem usado no script não contém nenhuma ocorrência de `<text` (confirme relendo o script antes de descartá-lo)
    - `git status --porcelain` não lista nenhum arquivo `.js`/`.mjs` de script gerador dentro do repositório
    - Nenhum arquivo em `src/` ganhou comentário
  </acceptance_criteria>
  <done>Os dois placeholders existem com dimensões e peso exatos, feitos só de formas vetoriais, e o script gerador ficou fora do repositório.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| registro npm → árvore de dependências local | código de terceiro entra no build |
| arquivo de imagem local → pipeline Sharp no build | dado binário processado por biblioteca nativa em tempo de build |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-SC | Tampering | `pnpm add -D sharp@0.35.4` | mitigate | RESEARCH.md § Package Legitimacy Audit dá veredito `[OK]` (slopcheck, 12+ anos no registro, repo `github.com/lovell/sharp`); a versão é idêntica à já resolvida e auditada limpa em `pnpm-lock.yaml`, então a árvore resolvida não muda — só a declaração. Task 1 exige `pnpm why sharp` antes da instalação e `pnpm audit --audit-level=high` depois. |
| T-03-01 | Tampering | `astro.config.mjs` (não modificado) | mitigate | Proibição explícita de habilitar `imageService: true` no adapter da Vercel ou `passthroughImageService()`: manter a otimização 100% em build elimina uma origem de imagem de terceiro que a CSP estrita da Fase 7 teria de permitir. Verificado por `git diff --name-only` na acceptance criteria. |
| T-03-02 | Tampering / Information Disclosure | pipeline `astro:assets` | mitigate | Nenhum `image.domains` / `image.remotePatterns` é configurado; os dois assets desta fase são arquivos locais autorados sob `src/assets/`, nunca URLs remotas — `<Image>`/`<Picture>` não podem ser apontados para host controlado por atacante. |
| T-03-03 | Information Disclosure | rasterização dos placeholders | accept | Placeholders são formas vetoriais geradas pelo próprio build, sem metadados EXIF de câmera, sem PII. Risco residual nulo. |
</threat_model>

<verification>
- `pnpm build` continua saindo com código 0 (nenhum componente consome imagem ainda neste plano; o objetivo é não regredir).
- `bash scripts/security-check.sh --ci` mantém o mesmo número de PASS de antes do plano.
</verification>

<success_criteria>
- `grep '"sharp": "0.35.4"' package.json` retorna match e `pnpm why sharp` não mostra segunda versão.
- Os dois `.webp` em `src/assets/` passam a checagem automatizada de formato/dimensão/peso da Task 2.
- `astro.config.mjs`, `pnpm-workspace.yaml` e todo o resto de `src/` permanecem inalterados.
</success_criteria>

<output>
Create `.planning/phases/03-static-zero-js-sections-csp-safe-refactor-a11y/03-01-SUMMARY.md` when done
</output>
