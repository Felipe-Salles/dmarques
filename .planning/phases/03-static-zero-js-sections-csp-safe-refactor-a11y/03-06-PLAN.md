---
phase: 03-static-zero-js-sections-csp-safe-refactor-a11y
plan: 06
type: execute
wave: 2
depends_on: ["03-02"]
files_modified:
  - src/components/DifferentiatorsSection.astro
  - src/components/FaqSection.astro
autonomous: true
requirements: [SITE-01, SITE-03, SITE-04, A11Y-02, A11Y-06, ANIM-02, ANIM-07]

must_haves:
  truths:
    - "A seção Diferenciais renderiza os 4 itens da collection sobre fundo claro, com numeração 01 a 04 derivada de order"
    - "A seção FAQ renderiza as 4 perguntas e respostas da collection faq"
    - "Todo texto sobre o fundo claro #F2F3F6 atinge contraste AA"
    - "Nenhuma das duas seções embarca JavaScript de cliente"
  artifacts:
    - path: "src/components/DifferentiatorsSection.astro"
      provides: "Seção Diferenciais (única seção de fundo claro da página)"
      contains: "getDifferentiators"
    - path: "src/components/FaqSection.astro"
      provides: "Seção FAQ, fonte única compartilhada com o JSON-LD da Fase 6"
      contains: "getFaq"
  key_links:
    - from: "src/components/DifferentiatorsSection.astro"
      to: "src/content/index.ts"
      via: "import { getDifferentiators } e derivação de numeração a partir de data.order"
      pattern: "padStart"
    - from: "src/components/FaqSection.astro"
      to: "src/content/index.ts"
      via: "import { getFaq }"
      pattern: "getFaq"
---

<objective>
Autorar a seção Diferenciais (a única de fundo claro da página, com sua própria
matemática de contraste) e a seção FAQ.

Purpose: Diferenciais é onde o RESEARCH encontrou as piores falhas de contraste do
design — inclusive uma cor `#9096A8` que nunca foi token e falha até o limite de 3:1
para texto grande. FAQ é a fonte única que a Fase 6 vai reusar no JSON-LD `FAQPage`,
então a estrutura de heading precisa nascer certa.
Output: `DifferentiatorsSection.astro` + `FaqSection.astro`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-static-zero-js-sections-csp-safe-refactor-a11y/03-UI-SPEC.md
@.planning/phases/03-static-zero-js-sections-csp-safe-refactor-a11y/03-PATTERNS.md
@.planning/phases/03-static-zero-js-sections-csp-safe-refactor-a11y/03-RESEARCH.md
</context>

<interfaces>
Contratos de dados já existentes (Fase 2):

- `src/content/index.ts` exporta `getDifferentiators()` e `getFaq()`, ambos `async`,
  ambos já ordenados por `data.order`.
- Schema `differentiators`: `order` (number), `titulo` (string), `descricao` (string).
  **NÃO possui campo `numero`** — os rótulos "01".."04" do design têm de ser derivados
  com `String(item.data.order).padStart(2, '0')` (Pitfall 8 do RESEARCH).
- Schema `faq`: `order` (number), `pergunta` (string), `resposta` (string, texto plano
  sem Markdown — a Fase 6 alimenta o JSON-LD `FAQPage` a partir da mesma fonte).

Tokens relevantes para a seção clara (já em `tokens.css`, sem alteração neste plano):
`--color-light-bg` #F2F3F6, `--color-light-surface` #FFFFFF, `--color-light-text`
#3D4253 (9.0:1), `--color-light-text-muted` #565C6E (6.0:1), `--color-light-border`,
`--color-accent` #6C4CFF (4.599:1 contra #F2F3F6 — PASSA mesmo como texto pequeno).
`--color-light-text-faint` #767C8E mede 3.75:1 e NÃO pode ser usado para texto de
corpo neste fundo.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: DifferentiatorsSection.astro — grade clara com numeração derivada e contraste corrigido</name>
  <files>src/components/DifferentiatorsSection.astro</files>
  <read_first>
    - src/content/index.ts (assinatura de `getDifferentiators`)
    - src/content.config.ts (schema `differentiators` — confirme com os próprios olhos que NÃO existe campo `numero`)
    - src/content/differentiators/atendimento-direto.yaml (forma real dos dados)
    - arquivos de design/Dmarques Landing.dc.html linhas 276 a 306 (seção Diferenciais: coluna intro sticky com eyebrow, h2 em duas linhas com span, parágrafo; grade de 4 tiles com número, h3 e descrição, hover mudando o fundo para branco)
    - src/styles/tokens.css (família `--color-light-*`)
    - .planning/phases/03-static-zero-js-sections-csp-safe-refactor-a11y/03-UI-SPEC.md seção "Contrast remediation" (linhas sobre `#9096A8` e `--color-light-text-faint`)
    - .planning/phases/03-static-zero-js-sections-csp-safe-refactor-a11y/03-RESEARCH.md Pitfall 2 (tabela do fundo claro) e Pitfall 8 (numeração derivada)
  </read_first>
  <action>
    Crie `src/components/DifferentiatorsSection.astro`.

    Frontmatter: `import { getDifferentiators } from '../content'` e
    `const differentiators = await getDifferentiators()`.

    `<section id="diferenciais">` com fundo `var(--color-light-bg)`. Esta é a única
    seção clara da página; todo texto dentro dela usa a família `--color-light-*`,
    nunca os tokens de branco translúcido.

    Coluna de intro (com `data-reveal="1"`, `position: sticky; top: var(--space-10)`
    a partir do breakpoint de duas colunas):
    - Eyebrow `"Diferenciais"` em `var(--color-accent)`. Aqui o acento PERMANECE
      `#6C4CFF`: contra `#F2F3F6` ele mede 4.599:1 e passa mesmo como texto pequeno —
      a matemática do fundo claro é diferente da do fundo escuro, e trocar por
      `--color-accent-light` aqui pioraria o contraste.
    - `<h2>Por que trabalhar<br><span>com a gente</span></h2>`. CORREÇÃO OBRIGATÓRIA:
      o design usa a cor literal `#9096A8` nesse `<span>` (linha 280). Essa cor mede
      2.661:1 contra `#F2F3F6` e falha até o limite de 3:1 para texto grande; ela nunca
      foi token do projeto. Substitua por `var(--color-light-text-muted)` (#565C6E,
      6.0:1). NÃO invente um tom intermediário novo: já existe token que passa.
    - Parágrafo de apoio verbatim da linha 281, em `var(--color-light-text-muted)`.

    Grade de tiles: `{differentiators.map((item) => ...)}` produzindo um `<article
    data-reveal="1">` por item com:
    - O rótulo numérico obtido por `String(item.data.order).padStart(2, '0')`. NUNCA
      escreva "01".."04" como literal nem adicione campo ao YAML: a collection não tem
      `numero` e derivar do `order` mantém uma única fonte de verdade quando alguém
      reordenar ou inserir um item. Cor: `var(--color-accent)`.
    - `<h3>{item.data.titulo}</h3>` em `var(--color-light-text-strong)`.
    - `<p>{item.data.descricao}</p>` em `var(--color-light-text-muted)`.
      NÃO use `var(--color-light-text-faint)` para este parágrafo: 3.75:1 falha AA
      para texto de corpo.
    - Hover do tile: o design muda o fundo de `#F2F3F6` para `#fff`. `background` não
      está na lista permitida de propriedades de transição do ANIM-07
      (`transform`/`opacity`/`box-shadow`/`border-color`). Troque a transição de
      `background` por `box-shadow` — aplique `background: var(--color-light-surface)`
      instantaneamente e transicione apenas `box-shadow` (e, se quiser, `border-color`)
      com `var(--dur-slow)`. Regra agrupada `:hover, :focus-within`.

    Nenhum `style=`, `style-hover=`, `{{ }}`, `onClick`. Só `var(--token)`, zero hex
    literal, zero comentário.
  </action>
  <verify>
    <automated>pnpm check && pnpm build && node -e "const c=require('fs').readFileSync('src/components/DifferentiatorsSection.astro','utf8');const need=['getDifferentiators','id=\"diferenciais\"','data-reveal=\"1\"',\"padStart(2, '0')\",'item.data.titulo','item.data.descricao','--color-light-bg','--color-light-text-muted','<h2','<h3'];const m=need.filter(x=>!c.includes(x));if(m.length){console.error('FALTANDO',m);process.exit(1)};if(c.includes('--color-light-text-faint')){console.error('token reprovado em uso');process.exit(1)};if(/transition[^;]*background/.test(c)){console.error('transicao em background viola ANIM-07');process.exit(1)};const bad=['style-hover','style-focus','onClick','{{ '];const b=bad.filter(x=>c.includes(x));if(b.length){console.error('PROIBIDO',b);process.exit(1)};if(/#[0-9a-fA-F]{6}/.test(c)){console.error('HEX LITERAL');process.exit(1)};if(c.includes('/*')){console.error('COMENTARIO');process.exit(1)};console.log('Differentiators OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm check` e `pnpm build` saem com código 0
    - O rótulo numérico é produzido por `String(item.data.order).padStart(2, '0')`; nenhuma string literal `"01"`, `"02"`, `"03"` ou `"04"` aparece no arquivo
    - `grep -c '9096A8' src/components/DifferentiatorsSection.astro` retorna 0 e o `<span>` do `<h2>` usa `var(--color-light-text-muted)`
    - `grep -c 'color-light-text-faint' src/components/DifferentiatorsSection.astro` retorna 0
    - Nenhuma regra `transition` menciona `background`
    - Existe exatamente um `<h2` e um `<h3` por tile
    - Nenhuma ocorrência de `style-hover`, `style-focus`, `onClick` ou `{{ `
    - Nenhum hex literal de 6 dígitos e nenhum comentário
  </acceptance_criteria>
  <done>Diferenciais renderiza os 4 tiles da collection com numeração derivada, todas as cores do fundo claro passando AA e transição restrita às propriedades permitidas.</done>
</task>

<task type="auto">
  <name>Task 2: FaqSection.astro — 4 pares pergunta/resposta como fonte única</name>
  <files>src/components/FaqSection.astro</files>
  <read_first>
    - src/content/index.ts (assinatura de `getFaq`)
    - src/content.config.ts (schema `faq`: campos `pergunta` e `resposta`)
    - src/content/faq/prazo-projeto.yaml (forma real dos dados)
    - arquivos de design/Dmarques Landing.dc.html linhas 389 a 430 (seção FAQ: eyebrow "Perguntas frequentes", h2 com span, e 4 pares de balões pergunta/resposta)
    - src/components/ServicesSection.astro (padrão de consumo de collection já estabelecido)
    - src/styles/tokens.css e src/styles/base.css
    - .planning/phases/03-static-zero-js-sections-csp-safe-refactor-a11y/03-UI-SPEC.md seção "Component Inventory" (regra travada: cada pergunta é um `<h3>`)
  </read_first>
  <action>
    Crie `src/components/FaqSection.astro`.

    Frontmatter: `import { getFaq } from '../content'` e
    `const faq = await getFaq()`.

    `<section id="faq">` com fundo `var(--color-bg)`.

    Bloco de intro com `data-reveal="1"`: eyebrow `"Perguntas frequentes"` e
    `<h2>As dúvidas que <span>sempre aparecem</span></h2>`. O eyebrow usa
    `var(--color-accent-light)` — o design usa `#6C4CFF` cru na linha 392, que falha
    4.5:1 como texto pequeno sobre `#0A0A12`; esta é a terceira das instâncias que a
    UI-SPEC manda migrar (D-14). O `<span>` do `<h2>` é texto grande e permanece em
    `var(--color-accent)`.

    Pares Q/A: `{faq.map((item) => ...)}` produzindo um `<article data-reveal="1">`
    por par com:
    - `<h3>{item.data.pergunta}</h3>` — decisão travada na UI-SPEC: a pergunta é um
      heading de nível 3, não um `<p>` como no design. Isso mantém a seção navegável
      por heading em leitor de tela e alinha a estrutura com o `FAQPage` que a Fase 6
      vai gerar da mesma collection.
    - `<p>{item.data.resposta}</p>`.
    - Os dois balões mantêm o tratamento visual do design: balão da pergunta com
      `var(--color-surface-raised)` e borda `var(--color-border)`; balão da resposta
      deslocado, com borda em `var(--color-accent-border)` e fundo
      `var(--color-accent-surface)`. `border-radius: var(--radius-xl)`.

    NÃO use `<details>`/`<summary>` como acordeão aqui. O design mostra todas as
    respostas abertas, e colapsá-las esconderia conteúdo que a Fase 6 vai declarar como
    visível no `FAQPage` — divergência entre o JSON-LD e o que o usuário vê é
    exatamente o que o Rich Results Test reprova.

    NÃO interpole `resposta` via `set:html`: é texto plano por schema e deve entrar
    como nó de texto escapado.

    Nenhum `style=`, `style-hover=`, `{{ }}`, `onClick`. Só `var(--token)`, zero hex
    literal, zero comentário.
  </action>
  <verify>
    <automated>pnpm check && pnpm build && node -e "const c=require('fs').readFileSync('src/components/FaqSection.astro','utf8');const need=['getFaq','id=\"faq\"','data-reveal=\"1\"','item.data.pergunta','item.data.resposta','<h2','<h3','--color-accent-light'];const m=need.filter(x=>!c.includes(x));if(m.length){console.error('FALTANDO',m);process.exit(1)};const bad=['set:html','<details','<summary','style-hover','style-focus','onClick','{{ '];const b=bad.filter(x=>c.includes(x));if(b.length){console.error('PROIBIDO',b);process.exit(1)};if(/#[0-9a-fA-F]{6}/.test(c)){console.error('HEX LITERAL');process.exit(1)};if(c.includes('/*')){console.error('COMENTARIO');process.exit(1)};console.log('Faq OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm check` e `pnpm build` saem com código 0
    - Cada pergunta é renderizada dentro de `<h3>` e cada resposta dentro de `<p>`
    - `grep -c 'set:html' src/components/FaqSection.astro` retorna 0
    - `grep -cE '<details|<summary' src/components/FaqSection.astro` retorna 0
    - O eyebrow usa `var(--color-accent-light)`
    - Nenhum texto de pergunta ou resposta aparece como string literal no `.astro` — tudo vem de `item.data.*`
    - Nenhuma ocorrência de `style-hover`, `style-focus`, `onClick` ou `{{ `
    - Nenhum hex literal de 6 dígitos e nenhum comentário
  </acceptance_criteria>
  <done>FAQ renderiza os 4 pares da collection com pergunta como `<h3>`, respostas sempre visíveis e escapadas, pronta para a Fase 6 reusar a mesma fonte no JSON-LD.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Content Collection (YAML validado por Zod em build) -> markup renderizado | dados de conteúdo entram no HTML |
| conteúdo visível da FAQ -> JSON-LD da Fase 6 | divergência entre os dois vira problema de conformidade de rich results |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-11 | Tampering (XSS) | interpolação de `pergunta`/`resposta`/`titulo`/`descricao` | mitigate | Interpolação exclusivamente como `{item.data.campo}` (nó de texto escapado pelo Astro); `set:html` é proibido e verificado por grep no critério de aceite. Fecha o achado Low P02-003 herdado da Fase 2 para os campos desta fase. |
| T-03-14 | Repudiation | divergência FAQ visível vs. JSON-LD futuro | mitigate | Respostas permanecem sempre visíveis (sem acordeão) e vêm da mesma collection que a Fase 6 vai consumir, garantindo que o `FAQPage` declare apenas conteúdo efetivamente exibido. |
| T-03-01 | Tampering | ambos os componentes | mitigate | Zero `style=`, zero `<script>`, zero handler inline; não amplia a superfície que o `style-src`/`script-src` estrito da Fase 7 terá de permitir. Verificado por `security-check.sh` verificacao 2. |
| T-03-15 | Denial of Service | seção clara com `position: sticky` | accept | `sticky` é recurso nativo de layout sem custo de script; sem risco de travamento. Nenhuma mitigação necessária. |
</threat_model>

<verification>
- `pnpm check` e `pnpm build` saem com código 0.
- `bash scripts/security-check.sh --ci` mantém `PASS: verificacao 2`.
- `grep -rn 'set:html' src/` não retorna nada.
- `grep -rn '9096A8' src/` não retorna nada.
</verification>

<success_criteria>
- Diferenciais e FAQ renderizam 100% a partir das collections, sem copy duplicada.
- A cor não-token `#9096A8` do design não existe em lugar nenhum de `src/`.
- Nenhuma transição em `background` ou em propriedade que dispara layout.
- Perguntas da FAQ são `<h3>` e respostas ficam sempre visíveis.
</success_criteria>

<output>
Create `.planning/phases/03-static-zero-js-sections-csp-safe-refactor-a11y/03-06-SUMMARY.md` when done
</output>
