---
phase: 03-static-zero-js-sections-csp-safe-refactor-a11y
plan: 07
type: execute
wave: 2
depends_on: ["03-02"]
files_modified:
  - src/components/ContactSection.astro
  - src/components/CtaFinal.astro
  - src/components/SiteFooter.astro
autonomous: true
requirements: [SITE-01, SITE-03, SITE-04, A11Y-02, A11Y-04, A11Y-06, ANIM-02, ANIM-07]

must_haves:
  truths:
    - "O formulário de contato tem os 5 campos com label for, id e name reais e é operável por teclado"
    - "O formulário não possui action, method nem qualquer handler de JavaScript"
    - "As opções de Tipo de projeto vêm do mesmo enum usado pela futura validação do servidor"
    - "O rodapé apresenta navegação, contato e NAP com todo link externo protegido por rel noopener noreferrer"
  artifacts:
    - path: "src/components/ContactSection.astro"
      provides: "Seção Contato com o formulário estático de 5 campos (D-07; D-08: required só em Nome e Tipo de projeto; D-09: textarea Mensagem; D-10: label de submit estático; D-11: sem action/method)"
      contains: "PROJECT_TYPES"
    - path: "src/components/CtaFinal.astro"
      provides: "Bloco de chamada final"
    - path: "src/components/SiteFooter.astro"
      provides: "Rodapé com navegação, contato, NAP e watermark decorativo"
  key_links:
    - from: "src/components/ContactSection.astro"
      to: "src/content/project-types.ts"
      via: "import PROJECT_TYPES alimentando as <option> do select"
      pattern: "PROJECT_TYPES"
    - from: "src/components/SiteFooter.astro"
      to: "seções da home"
      via: "âncoras #servicos #processo #diferenciais #sobre"
      pattern: "href=\"#diferenciais\""
---

<objective>
Autorar as três últimas peças da página: a seção Contato com o formulário estático de
5 campos, o bloco de CTA final e o rodapé.

Purpose: o formulário é onde a acessibilidade de formulário desta fase é decidida
(`<label for>`/`id`/`name` reais, operabilidade por teclado, anel de foco em fundo
claro) e onde D-07 a D-11 delimitam com precisão o que é Fase 3 e o que é Fase 5.
Ship agora com os 5 campos significa que a Fase 5 só acrescenta `action`, `method`,
validação e enhancement, sem nunca reabrir uma revisão de acessibilidade.
Output: `ContactSection.astro` + `CtaFinal.astro` + `SiteFooter.astro`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-static-zero-js-sections-csp-safe-refactor-a11y/03-UI-SPEC.md
@.planning/phases/03-static-zero-js-sections-csp-safe-refactor-a11y/03-PATTERNS.md
@.planning/phases/03-static-zero-js-sections-csp-safe-refactor-a11y/03-CONTEXT.md
</context>

<interfaces>
Contrato de dados existente (Fase 2), consumido pelo `<select>`:

- `src/content/project-types.ts` exporta `PROJECT_TYPE_VALUES` (tupla `as const` com
  `site-institucional`, `sistema-sob-medida`, `ecommerce-plataforma`, `automacao`,
  `nao-sei`) e `PROJECT_TYPES` (array `as const` de objetos `{ value, label }` com os
  rótulos em pt-BR). Este é o MESMO enum que a validação Zod do endpoint da Fase 5 vai
  usar como allow-list (CONTENT-05) — o `<select>` precisa derivar dele, nunca
  reescrever as opções à mão.

Contratos globais já disponíveis (plano 03-02): `:focus-visible` global com
`outline: 3px solid var(--color-accent)`, token `--focus-ring` opaco para uso em
box-shadow nos inputs, `[data-reveal]` visível por padrão, guarda de reduced-motion.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: ContactSection.astro — coluna de argumentos + formulário estático de 5 campos</name>
  <files>src/components/ContactSection.astro</files>
  <read_first>
    - src/content/project-types.ts (arquivo inteiro: nomes exatos dos exports, valores e rótulos)
    - arquivos de design/Dmarques Landing.dc.html linhas 332 a 388 (seção Contato: coluna esquerda com eyebrow "Orçamento", h2, três bullets com check, telefone e linha de e-mail/cidade; coluna direita com o card de formulário claro de 3 campos e sintaxe de editor a descartar)
    - src/styles/tokens.css (família `--color-light-*`, `--color-light-input-border`, `--focus-ring`, `--radius-md`)
    - .planning/phases/03-static-zero-js-sections-csp-safe-refactor-a11y/03-UI-SPEC.md seções "Contact Form Field Contract", "Copywriting Contract" e "Contrast remediation"
    - .planning/phases/03-static-zero-js-sections-csp-safe-refactor-a11y/03-CONTEXT.md D-07 a D-11
  </read_first>
  <action>
    Crie `src/components/ContactSection.astro`.

    Frontmatter: `import { PROJECT_TYPES } from '../content/project-types'`. Nenhuma
    outra lógica.

    `<section id="contato">` com o gradiente vertical de `var(--color-bg)` para
    `var(--color-bg-blue)` (o design usa os hex crus; use tokens). O `<div>` de glow
    decorativo da linha 333 entra com `aria-hidden="true"`; o `overflow: hidden` fica
    escopado a esse container, nunca à seção inteira nem a um wrapper de página.

    Coluna esquerda (`data-reveal="1"`):
    - Eyebrow `"Orçamento"` — o design já usa `#9A85FF` aqui; mapeie para
      `var(--color-accent-light)`.
    - `<h2>Vamos falar sobre o <span>seu projeto</span></h2>` com o span em
      `var(--color-accent)` (texto grande, passa na exceção 3:1).
    - Lista de três benefícios: use `<ul>` real com `<li>`, não `<div>`s. Cada item
      tem o SVG de check (`aria-hidden="true"`, `stroke="currentColor"` colorido via
      CSS em `var(--color-accent)` — o design usa `stroke="#6C4CFF"` cru, troque) e os
      textos `"Conversa sem compromisso"`, `"Proposta clara, com escopo e prazo"`,
      `"Início rápido"`.
    - Link de telefone `<a href="https://wa.me/5516996111785">` com o SVG do WhatsApp
      (`aria-hidden="true"`) e o texto `"(16) 99611-1785"`, com `target="_blank"` e
      `rel="noopener noreferrer"` (T-03-04). NÃO adicione `?text=` (FORM-13 é Fase 5).
    - Linha `"felipe.salles1@hotmail.com · Ibitinga, SP"`. Use
      `var(--color-text-muted)` — o design usa `rgba(255,255,255,.5)`, abaixo do
      mínimo AA para texto de corpo sobre os fundos escuros.

    Coluna direita — o card de formulário, fundo claro `var(--color-light-bg-alt)`,
    `border-radius: var(--radius-2xl)`, `box-shadow: var(--shadow-lg)`, com
    `data-reveal="1"`:
    - Cabeçalho do card: `<h3>Pedir orçamento</h3>` e o helper
      `"Resposta no mesmo dia útil."`. CORREÇÃO OBRIGATÓRIA: esse helper é texto de
      13.5px sobre fundo claro e o design o pinta com o equivalente a
      `--color-light-text-faint` (#767C8E, 3.75:1 — FALHA AA). Use
      `var(--color-light-text-muted)` (#565C6E, 6.0:1). Não altere o token base:
      D-14 manda corrigir o uso que falha.
    - `<form>` SEM atributo `action`, SEM `method`, SEM `onsubmit`, SEM `id` de
      binding de JS (D-11 — o endpoint `/api/orcamento` é FORM-02, escopo da Fase 5).
      O `onSubmit="{{ onSubmit }}"` da linha 361 do design é sintaxe do editor e é
      PROIBIDO transplantar.
    - Cinco campos, cada um com `<label for>` real apontando para o `id` do controle,
      e `name` igual ao `id`, nesta ordem:
      1. Nome — `<input type="text" id="nome" name="nome" required>`, placeholder
         `"Como podemos te chamar"`.
      2. WhatsApp — `<input type="tel" id="whatsapp" name="whatsapp">` SEM `required`,
         placeholder `"(00) 00000-0000"`.
      3. Logo abaixo do WhatsApp, o aviso da regra "pelo menos um":
         `"Preencha ao menos um: WhatsApp ou e-mail."` em `--text-xs` e
         `var(--color-light-text-muted)`. Dê a esse elemento um `id` e referencie-o
         por `aria-describedby` nos DOIS campos (WhatsApp e E-mail) para que leitores
         de tela anunciem a regra em ambos.
      4. E-mail — `<input type="email" id="email" name="email">` SEM `required`.
         Campo NOVO, não existe no design (D-07); estilize igual aos demais.
      5. Tipo de projeto — `<select id="tipo-projeto" name="tipo-projeto" required>`
         com as `<option>` geradas por `{PROJECT_TYPES.map((t) => <option
         value={t.value}>{t.label}</option>)}`. NÃO escreva as cinco opções à mão: o
         design as tem como texto literal sem `value`, o que quebraria a allow-list da
         Fase 5 (CONTENT-05).
      6. Mensagem — `<textarea id="mensagem" name="mensagem" rows="4">`, placeholder
         `"Conte um pouco sobre o seu projeto"`, `resize: vertical` no CSS. Campo
         NOVO (D-09).
      Apenas `Nome` e `Tipo de projeto` levam `required` (D-08). Marcar WhatsApp ou
      E-mail como `required` obrigaria os dois, contrariando FORM-01.
    - `<button type="submit">Enviar pedido de orçamento</button>` — rótulo estático
      (D-10). O `{{ submitLabel }}` do design é estado dinâmico da Fase 5.
    - Foco nos controles: sobre o card claro, use
      `box-shadow: var(--focus-ring)` em `:focus-visible` (o token agora é opaco e
      mede 5.10:1 contra branco), com `border-color: var(--color-accent)`. Transição
      apenas em `box-shadow, border-color`, `var(--dur-fast)`.
    - Botão: regra agrupada `:hover, :focus-visible` levando a
      `var(--color-accent-hover)`.

    Nenhum `style=`, `style-hover=`, `style-focus=`, `onSubmit`, `{{ }}`. Só
    `var(--token)`, zero hex literal, zero comentário.
  </action>
  <verify>
    <automated>pnpm check && pnpm build && node -e "const c=require('fs').readFileSync('src/components/ContactSection.astro','utf8');const need=['PROJECT_TYPES','id=\"contato\"','<form','for=\"nome\"','id=\"nome\"','name=\"nome\"','for=\"whatsapp\"','id=\"whatsapp\"','for=\"email\"','id=\"email\"','type=\"email\"','for=\"tipo-projeto\"','id=\"tipo-projeto\"','for=\"mensagem\"','id=\"mensagem\"','<textarea','aria-describedby','Preencha ao menos um: WhatsApp ou e-mail.','Enviar pedido de orçamento','rel=\"noopener noreferrer\"','--color-light-text-muted','--focus-ring'];const m=need.filter(x=>!c.includes(x));if(m.length){console.error('FALTANDO',m);process.exit(1)};const bad=['action=','method=','onSubmit','onsubmit','style-hover','style-focus','{{ ','--color-light-text-faint'];const b=bad.filter(x=>c.includes(x));if(b.length){console.error('PROIBIDO',b);process.exit(1)};if((c.match(/required/g)||[]).length!==2){console.error('required deve aparecer exatamente 2x (nome e tipo-projeto)');process.exit(1)};if(/#[0-9a-fA-F]{6}/.test(c)){console.error('HEX LITERAL');process.exit(1)};if(c.includes('/*')){console.error('COMENTARIO');process.exit(1)};console.log('Contact OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm check` e `pnpm build` saem com código 0
    - Os cinco campos existem com `for`/`id`/`name` casados: `nome`, `whatsapp`, `email`, `tipo-projeto`, `mensagem`
    - `required` aparece exatamente duas vezes no arquivo (Nome e Tipo de projeto)
    - O `<form>` não tem `action`, `method`, `onsubmit` nem `onSubmit`
    - As `<option>` são geradas por `PROJECT_TYPES.map`; nenhum rótulo como `Site institucional` aparece como literal fora do import
    - O aviso "pelo menos um" tem `id` e é referenciado por `aria-describedby` nos campos WhatsApp e E-mail
    - `grep -c 'color-light-text-faint' src/components/ContactSection.astro` retorna 0
    - O link de WhatsApp tem `rel="noopener noreferrer"` e não contém `?text=`
    - Nenhum hex literal de 6 dígitos, nenhum comentário, nenhuma sintaxe de editor
  </acceptance_criteria>
  <done>Contato entrega o formulário estático de 5 campos com rotulagem acessível, allow-list vinda do enum compartilhado, foco com contraste no card claro e zero wiring de Fase 5.</done>
</task>

<task type="auto">
  <name>Task 2: CtaFinal.astro — bloco de chamada final</name>
  <files>src/components/CtaFinal.astro</files>
  <read_first>
    - arquivos de design/Dmarques Landing.dc.html linhas 432 a 443 (CTA final: glow radial decorativo, h2 com span destacado, parágrafo e botão de WhatsApp)
    - src/components/HeroBleed.astro (tratamento visual do CTA primário já estabelecido — reaproveite a mesma linguagem de pill/sombra)
    - src/styles/tokens.css e src/styles/base.css
    - .planning/phases/03-static-zero-js-sections-csp-safe-refactor-a11y/03-UI-SPEC.md seção "Typography" (linha "CTA-final h2" tem o mesmo peso visual do H1, intencionalmente)
  </read_first>
  <action>
    Crie `src/components/CtaFinal.astro` a partir das linhas 432 a 443 do design.

    `<section>` sem `id` (não é alvo de âncora), fundo `var(--color-bg)`, padding
    vertical maior que o das demais seções conforme o design.

    O `<div>` de glow radial decorativo da linha 433 entra com `aria-hidden="true"`,
    `pointer-events: none`, `z-index: var(--z-glow)`; o `overflow: hidden` necessário
    para conter o glow fica NESTE container, nunca em wrapper compartilhado
    (Pitfall 4).

    Bloco central com `data-reveal="1"`:
    - `<h2>Vamos construir algo <span>sólido</span> juntos</h2>` — `clamp(34px, 5vw,
      68px)`, `var(--font-weight-bold)`, `line-height: 1.02`. O span usa
      `var(--color-accent)`: é texto grande e se qualifica na exceção 3:1.
      Este `<h2>` tem o mesmo peso visual do `<h1>` do Hero por decisão de design
      (declaração de fechamento) — isso é intencional e não deve ser "normalizado".
    - `<p>Conte o que você precisa. Quem responde é quem vai desenvolver.</p> `em
      `var(--color-text-muted)`, `text-wrap: pretty`.
    - `<a href="https://wa.me/5516996111785">` com o texto `"Começar um projeto"` e o
      SVG de seta (`aria-hidden="true"`), estilo pill em `var(--color-accent)` com
      `var(--shadow-accent)`; `:hover, :focus-visible` agrupados levando a
      `var(--color-accent-hover)` e `var(--shadow-accent-lg)`. OBRIGATÓRIO
      `target="_blank"` com `rel="noopener noreferrer"`. Sem `?text=`.

    Nenhum `style=`, `style-hover=`, `{{ }}`. Só `var(--token)`, zero hex literal,
    zero comentário.
  </action>
  <verify>
    <automated>pnpm check && node -e "const c=require('fs').readFileSync('src/components/CtaFinal.astro','utf8');const need=['data-reveal=\"1\"','<h2','Começar um projeto','Conte o que você precisa','rel=\"noopener noreferrer\"','aria-hidden=\"true\"',':focus-visible','--color-accent'];const m=need.filter(x=>!c.includes(x));if(m.length){console.error('FALTANDO',m);process.exit(1)};const bad=['style-hover','style-focus','onClick','{{ ','?text='];const b=bad.filter(x=>c.includes(x));if(b.length){console.error('PROIBIDO',b);process.exit(1)};if(/#[0-9a-fA-F]{6}/.test(c)){console.error('HEX LITERAL');process.exit(1)};if(c.includes('/*')){console.error('COMENTARIO');process.exit(1)};console.log('CtaFinal OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm check` sai com código 0
    - O bloco central tem `data-reveal="1"`, um `<h2>` e o parágrafo verbatim
    - O link tem `rel="noopener noreferrer"` e não contém `?text=`
    - O glow decorativo tem `aria-hidden="true"` e `pointer-events: none`
    - `overflow: hidden` aparece somente em seletor de container de glow
    - A regra `:hover` do botão está agrupada com `:focus-visible`
    - Nenhum hex literal de 6 dígitos, nenhum comentário, nenhuma sintaxe de editor
  </acceptance_criteria>
  <done>CtaFinal.astro reproduz o bloco de fechamento com glow decorativo contido, tipografia de maior peso da página e link externo seguro.</done>
</task>

<task type="auto">
  <name>Task 3: SiteFooter.astro — navegação, contato, NAP e watermark decorativo</name>
  <files>src/components/SiteFooter.astro</files>
  <read_first>
    - arquivos de design/Dmarques Landing.dc.html linhas 444 a 473 (rodapé: wordmark + tagline, coluna "Navegação" com 4 âncoras, coluna "Contato" com telefone, e-mail, Instagram e cidade, linha divisória, copyright e a marca d'água gigante "DMARQUES")
    - src/components/SiteHeader.astro (wordmark já autorado — reaproveite a mesma linguagem visual, sem duplicar landmark de nav principal)
    - src/styles/tokens.css e src/styles/base.css
    - .planning/phases/03-static-zero-js-sections-csp-safe-refactor-a11y/03-CONTEXT.md seção "Specifics" (regra sobre o watermark)
  </read_first>
  <action>
    Crie `src/components/SiteFooter.astro` a partir das linhas 444 a 473 do design.

    `<footer>` como landmark (A11Y-02), fundo `var(--color-bg-black)`.

    Bloco de marca: o quadrado "D" e o wordmark "Dmarques" (texto real, não imagem),
    seguidos da tagline `"Sites, sistemas, soluções web e automações sob medida."` em
    `var(--color-text-muted)`.

    Coluna "Navegação": um `<nav aria-label="Navegação do rodapé">` — o `aria-label`
    é obrigatório porque a página já tem a navegação principal em `SiteHeader`, e dois
    landmarks `<nav>` sem rótulo distinto confundem a navegação por landmarks
    (A11Y-02/A11Y-04). Título da coluna `"Navegação"` como `<h2>` visualmente
    discreto? NÃO — o rodapé não entra na hierarquia de headings (regra travada na
    UI-SPEC); use um `<span>` ou `<p>` estilizado. Links: `#servicos` "Serviços",
    `#processo` "Como trabalhamos", `#diferenciais` "Diferenciais", `#sobre` "Sobre".

    Coluna "Contato": rótulo `"Contato"` (mesmo tratamento não-heading) e os itens:
    - `<a href="https://wa.me/5516996111785">(16) 99611-1785</a>` com
      `target="_blank"` e `rel="noopener noreferrer"`.
    - `<a href="mailto:felipe.salles1@hotmail.com">felipe.salles1@hotmail.com</a>` —
      `mailto:` NÃO recebe `target="_blank"` nem `rel="noopener"` (não abre aba de
      navegador, o atributo seria ruído).
    - `<a href="https://instagram.com/felipe.salles1">@felipe.salles1</a>` com
      `target="_blank"` e `rel="noopener noreferrer"`.
    - `"Ibitinga · SP"` como texto. Esta linha, o telefone e o e-mail formam o NAP que
      a Fase 6 (SEO-08) vai precisar replicar byte a byte no JSON-LD — mantenha a
      grafia exata do design para não criar divergência depois.

    Linha divisória decorativa com `aria-hidden="true"`.

    Copyright: `"© 2026 Dmarques Soluções Web. Todos os direitos reservados."` em
    `var(--color-text-faint)` (token já corrigido para 0.47 no plano 03-02).

    Marca d'água "DMARQUES": o texto gigante translúcido da linha 466 é PURAMENTE
    decorativo e recebe `aria-hidden="true"`. Ele NÃO entra na auditoria de contraste
    AA porque não é texto destinado à leitura (registrado em CONTEXT.md § Specifics).
    Mantenha o `overflow: hidden` que o contém escopado ao próprio `<footer>`.

    Todo link do rodapé tem regra agrupada `:hover, :focus-visible` levando a
    `var(--color-accent-light)` (texto pequeno sobre `#000` — `--color-accent` falharia
    4.5:1).

    Nenhum `style=`, `style-hover=`, `{{ }}`. Só `var(--token)`, zero hex literal,
    zero comentário.
  </action>
  <verify>
    <automated>pnpm check && pnpm build && node -e "const c=require('fs').readFileSync('src/components/SiteFooter.astro','utf8');const need=['<footer','aria-label=\"Navegação do rodapé\"','href=\"#servicos\"','href=\"#processo\"','href=\"#diferenciais\"','href=\"#sobre\"','wa.me/5516996111785','mailto:felipe.salles1@hotmail.com','instagram.com/felipe.salles1','Ibitinga · SP','© 2026 Dmarques Soluções Web','rel=\"noopener noreferrer\"','--color-accent-light',':focus-visible'];const m=need.filter(x=>!c.includes(x));if(m.length){console.error('FALTANDO',m);process.exit(1)};if(/<h[1-6]/.test(c)){console.error('rodape nao deve conter heading');process.exit(1)};const bad=['style-hover','style-focus','onClick','{{ '];const b=bad.filter(x=>c.includes(x));if(b.length){console.error('PROIBIDO',b);process.exit(1)};if(/#[0-9a-fA-F]{6}/.test(c)){console.error('HEX LITERAL');process.exit(1)};if(c.includes('/*')){console.error('COMENTARIO');process.exit(1)};console.log('Footer OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm check` e `pnpm build` saem com código 0
    - O `<nav>` do rodapé tem `aria-label="Navegação do rodapé"` distinto do nav principal
    - `grep -cE '<h[1-6]' src/components/SiteFooter.astro` retorna 0
    - Os links `wa.me` e `instagram.com` têm `rel="noopener noreferrer"`; o `mailto:` NÃO tem `rel="noopener"` nem `target="_blank"`
    - A marca d'água "DMARQUES" tem `aria-hidden="true"`
    - O NAP (`(16) 99611-1785`, `felipe.salles1@hotmail.com`, `Ibitinga · SP`) aparece com a grafia exata do design e está registrado literalmente no SUMMARY para a Fase 6 reusar
    - Nenhum hex literal de 6 dígitos, nenhum comentário, nenhuma sintaxe de editor
  </acceptance_criteria>
  <done>SiteFooter.astro entrega landmark de rodapé com navegação rotulada, NAP canônico, links externos seguros e watermark decorativo oculto para leitores de tela.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| campos de formulário -> (futuro) endpoint `/api/orcamento` | markup que a Fase 5 vai conectar a processamento no servidor |
| página -> destinos externos (wa.me, instagram.com, mailto) | navegação para origem de terceiro |
| enum de tipo de projeto -> allow-list de validação da Fase 5 | um único desvio aqui vira falha de validação depois |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-16 | Tampering | `<form>` de `ContactSection.astro` | mitigate | O formulário ship SEM `action`/`method`/handler (D-11): nenhum endpoint existe ainda, então não há superfície de submissão a abusar. A ausência dos atributos é verificada por grep no critério de aceite, impedindo que um executor "complete" a fiação antecipadamente sem as defesas da Fase 5 (honeypot, timing, rate limit, Zod). |
| T-03-17 | Tampering | `<select id="tipo-projeto">` | mitigate | As `<option>` derivam de `PROJECT_TYPES`, o mesmo módulo que a validação Zod da Fase 5 vai usar como allow-list (CONTENT-05). Escrever as opções à mão (como no design) criaria valores que o servidor rejeitaria. Verificado por grep de `PROJECT_TYPES.map`. |
| T-03-04 | Tampering | links `wa.me` e `instagram.com` em Contato, CTA final e Rodapé | mitigate | `target="_blank"` sempre acompanhado de `rel="noopener noreferrer"`: sem `noopener` o destino obtém `window.opener` e pode reescrever a aba de origem (tabnabbing). `mailto:` fica deliberadamente sem esses atributos. |
| T-03-18 | Information Disclosure | e-mail e telefone no rodapé | accept | São dados de contato comercial publicados intencionalmente pelo fundador; expostos em texto claro no HTML aceita-se o risco de coleta por bots. TRUST-01 (e-mail de domínio próprio) já está registrado como item pós-validação em REQUIREMENTS.md. |
| T-03-01 | Tampering | os três componentes | mitigate | Zero `style=`, zero `<script>`, zero handler inline — não amplia a superfície que o `script-src`/`style-src` estrito da Fase 7 terá de permitir. Verificado por `security-check.sh` verificacao 2 e por grep de `onSubmit`/`{{ `. |
</threat_model>

<verification>
- `pnpm check` e `pnpm build` saem com código 0.
- `bash scripts/security-check.sh --ci` mantém `PASS: verificacao 2`.
- `grep -rn 'onClick\|onSubmit\|{{ ' src/` não retorna nada.
- `grep -rn 'action=\|method=' src/components/ContactSection.astro` não retorna nada.
</verification>

<success_criteria>
- Formulário com 5 campos rotulados, `required` só em Nome e Tipo de projeto, opções vindas de `PROJECT_TYPES`, aviso "pelo menos um" ligado por `aria-describedby`.
- Zero fiação de Fase 5 (`action`, `method`, handler, estado dinâmico de botão).
- Todos os links externos com `rel="noopener noreferrer"`; `mailto:` sem eles.
- NAP registrado literalmente no SUMMARY para reuso em SEO-08.
- Zero `style=`, zero hex literal, zero comentário nos três arquivos.
</success_criteria>

<output>
Create `.planning/phases/03-static-zero-js-sections-csp-safe-refactor-a11y/03-07-SUMMARY.md` when done
</output>
