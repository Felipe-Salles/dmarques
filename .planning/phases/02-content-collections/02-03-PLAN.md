---
phase: 02-content-collections
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - .planning/security/runs/phase-02.md
autonomous: true
requirements: [CONTENT-01, CONTENT-02, CONTENT-03, CONTENT-04, CONTENT-05]

must_haves:
  truths:
    - "An unknown key in a content file provably aborts pnpm build, with the verbatim error recorded as evidence"
    - "A missing required field provably aborts pnpm build, proving the D-02 order guarantee"
    - "The working tree is byte-identical to its pre-test state after both probes"
    - "The SEC-07 phase-02 run file exists with the verbatim security-check.sh output and all judgement items answered"
    - "No finding in the phase-02 run is left High and Open"
  artifacts:
    - path: ".planning/security/runs/phase-02.md"
      provides: "SEC-07 phase-02 review record with verbatim script output, four judgement answers, and a P02-00x findings table"
      contains: "resumo:"
      min_lines: 60
  key_links:
    - from: ".planning/security/runs/phase-02.md"
      to: ".planning/security/SECURITY-CHECKLIST.md"
      via: "answers all 7 gabarito items"
      pattern: "SECURITY-CHECKLIST"
    - from: ".planning/security/runs/phase-02.md"
      to: "scripts/security-check.sh"
      via: "verbatim --ci stdout"
      pattern: "verificacao 1"
---

<objective>
Prove the strict-schema build gate actually fires, and file the SEC-07 review record for Phase 2.

Purpose: ROADMAP Phase 2 success criterion 1 ("a bad key or missing field fails `pnpm build`")
needs evidence, not an assertion; success criterion 4 needs a SEC-07 checklist run with no open
High finding. This plan produces both.
Output: recorded negative-test evidence in the SUMMARY, and
`.planning/security/runs/phase-02.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-content-collections/02-CONTEXT.md
@.planning/phases/02-content-collections/02-RESEARCH.md
@.planning/phases/02-content-collections/02-01-SUMMARY.md
@.planning/phases/02-content-collections/02-02-SUMMARY.md
@.planning/security/SECURITY-CHECKLIST.md
@CLAUDE.md
</context>

<interfaces>
<!-- scripts/security-check.sh contract, read from the Phase 1 script this session. -->
```
bash scripts/security-check.sh --ci
  env: STATIC_DIR (default .vercel/output/static), PREVIEW_URL (optional),
       VERCEL_AUTOMATION_BYPASS_SECRET (optional, used as the x-vercel-protection-bypass header)
  stdout lines: "PASS: verificacao N - ...", "FAIL: verificacao N - ...", "SKIP: verificacao N - ..."
  final line:   "== resumo: <P> PASS / <F> FAIL / <S> SKIP =="
  exit code:    0 when FAIL count is 0, otherwise 1
  checks:       1 pnpm audit · 2 style= in src/ · 3 inline CSS in built HTML ·
                4 secret scan of build output · 5 serverless Function count <= 1 ·
                6 curl -I preview headers (baseline) · 7 Lighthouse gate
  Without PREVIEW_URL, checks 6 and 7 SKIP — expected result for this phase is
  "== resumo: 5 PASS / 0 FAIL / 2 SKIP ==" with exit 0.
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Run the one-time negative tests and capture the evidence</name>
  <files>(no committed source change — both probes are reverted)</files>
  <read_first>
    - `.planning/phases/02-content-collections/02-RESEARCH.md` § "Negative test" (L651-658) and Assumption A3 (L685) — the probe this task executes and the claim it settles
    - `.planning/phases/02-content-collections/02-PATTERNS.md` § "Strict schema = hard build failure" (L323-332)
    - `src/content.config.ts` — the strict schemas under test
    - `src/content/faq/prazo-projeto.yaml` and `src/content/services/solucoes-web.yaml` — the two probe targets
  </read_first>
  <action>
    Run two negative probes against the committed content, capture the verbatim failure output for the SUMMARY, and revert both. Nothing from this task is committed as source — the validation strategy (02-VALIDATION.md) deliberately adds no test framework; the build IS the validator, and this is its one-time proof.
    Probe A — unknown key (CONTENT-01 "a bad key fails the build"): append a `bogus_key: true` line to `src/content/faq/prazo-projeto.yaml`, run `pnpm build` with output tee'd to a scratchpad log, record the non-zero exit code and the verbatim Zod/Astro error line (expect wording along the lines of "Unrecognized key(s) in object: 'bogus_key'" naming the `faq` collection and the entry id), then `git checkout -- src/content/faq/prazo-projeto.yaml`.
    Probe B — missing required field (D-02 "pnpm build fails if order is missing"): delete the `order:` line from `src/content/services/solucoes-web.yaml`, run `pnpm build` tee'd to a second scratchpad log, record the non-zero exit code and the verbatim "required"/"invalid type" error naming `order`, then `git checkout -- src/content/services/solucoes-web.yaml`.
    After both reverts, run a clean `pnpm build` and confirm exit 0, then confirm `git status --porcelain src/` prints nothing — the tree must be byte-identical to its pre-test state.
    Write both verbatim error excerpts into `.planning/phases/02-content-collections/02-03-SUMMARY.md` as the evidence for ROADMAP Phase 2 success criterion 1. Keep the excerpts short (the error block, not the whole build log) and redact nothing — there are no secrets in a content-schema error.
    If either probe exits 0, STOP and report: the strict-schema gate is not actually firing, which invalidates CONTENT-01, and `src/content.config.ts` needs a fix (most likely `z.object(...)` instead of `z.strictObject(...)`, or a stray `.default()` / `.optional()` on the field under test) before the phase can close.
    Do not leave any `.tmp`, `.orig` or log file inside the repo — scratchpad only.
  </action>
  <verify>
    <automated>bash -c 'printf "\nbogus_key: true\n" >> src/content/faq/prazo-projeto.yaml; pnpm build >/dev/null 2>&1; A=$?; git checkout -- src/content/faq/prazo-projeto.yaml; grep -v "^order: " src/content/services/solucoes-web.yaml > .neg.tmp && mv .neg.tmp src/content/services/solucoes-web.yaml; pnpm build >/dev/null 2>&1; B=$?; git checkout -- src/content/services/solucoes-web.yaml; pnpm build >/dev/null 2>&1; C=$?; echo "unknown_key_rc=$A missing_field_rc=$B clean_rc=$C"; [ "$A" -ne 0 ] && [ "$B" -ne 0 ] && [ "$C" -eq 0 ]'</automated>
  </verify>
  <acceptance_criteria>
    - The verify command prints `unknown_key_rc=<non-zero> missing_field_rc=<non-zero> clean_rc=0` and exits 0
    - `git status --porcelain src/` prints nothing after the run (both probes fully reverted)
    - `ls .neg.tmp 2>/dev/null` prints nothing (no temp file left behind)
    - `02-03-SUMMARY.md` contains a verbatim excerpt of the Probe A error naming `bogus_key`
    - `02-03-SUMMARY.md` contains a verbatim excerpt of the Probe B error naming `order`
    - `pnpm build` exits 0 on the reverted tree
  </acceptance_criteria>
  <done>Both negative probes provably fail the build, the tree is clean afterwards, and both verbatim error excerpts are recorded in the SUMMARY as evidence for success criterion 1.</done>
</task>

<task type="auto">
  <name>Task 2: File the SEC-07 phase-02 security review run</name>
  <files>.planning/security/runs/phase-02.md</files>
  <read_first>
    - `.planning/security/SECURITY-CHECKLIST.md` — the 7 mechanical items and the 4 judgement items this run must answer, plus the `P<NN>-NNN` finding-ID convention
    - `.planning/security/runs/phase-01.md` — the exact template to mirror (header block, verbatim stdout section, judgement answers, findings table). Read the header and the judgement/findings sections; do not re-read the whole 630-line Lighthouse dump.
    - `scripts/security-check.sh` — the script under execution; it must NOT be modified by this task
    - `.planning/phases/02-content-collections/02-RESEARCH.md` § "Security Domain" (L769-802) — the pre-computed answers this phase is expected to produce
    - `.planning/phases/02-content-collections/02-01-SUMMARY.md` and `02-02-SUMMARY.md` — the phase notes the judgement items reference
  </read_first>
  <action>
    Create `.planning/security/runs/phase-02.md` in pt-BR, mirroring the structure of `phase-01.md`.
    Generate the evidence first: run `pnpm install --frozen-lockfile`, then `pnpm build`, then `bash scripts/security-check.sh --ci` from the repo root. With no `PREVIEW_URL` set, checks 6 and 7 SKIP and the expected summary line is `== resumo: 5 PASS / 0 FAIL / 2 SKIP ==` with exit 0. Paste the complete stdout verbatim into the run file — do not summarize, reorder or edit it.
    Record the check 6 / check 7 SKIPs honestly and name the compensating control: the authoritative Lighthouse gate for this phase is the CI `lhci` job running against the Vercel preview for the phase-02 HEAD, exactly as recorded for Phase 1 (local check 7 additionally fails on this Windows machine due to the chrome-launcher EPERM + antivirus script injection documented in the phase-01 run). If a preview deploy for the phase-02 HEAD already exists, optionally re-run the script with `PREVIEW_URL` and `VERCEL_AUTOMATION_BYPASS_SECRET` set and paste that output too — never echo the bypass secret value into the file.
    Header block must record: phase name, execution date, the reviewed `main` HEAD commit SHA and subject, the preview URL (or "nenhum preview de fase 2 ainda publicado"), the gabarito path with a note that it was not modified, and the exact commands used to generate the output.
    Answer all four judgement items explicitly, grounded in this phase's actual diff:
    dependency justified? — **nenhuma dependência adicionada**; every API used (`defineCollection`, `glob`, `z`, `image()`, `getCollection`) is an internal export of the already-audited `astro@7.3.1`, whose bundled `zod@^4.5.4` entered `pnpm-lock.yaml` in Phase 1. State that `package.json` and `pnpm-lock.yaml` are unchanged and show the `git diff --stat` proof for those two paths.
    third-party asset? — **nenhum**; the only asset added is the locally generated `src/content/cases/dmarques-cover.webp`, processed at build by `astro:assets`/Sharp, with no remote image domain configured.
    new secret? — **nenhum**; Phase 2 declares no `astro:env` entry and check 4 finds no secret name or `re_` pattern in the build output.
    minor findings owned? — list each with owner Felipe Salles and a target date; carry forward the two accepted `extract-zip` dev/CI-only advisories from Phase 1 if they are still in the tree.
    Also carry forward the two latent render-side notes this phase creates for downstream phases, as Low findings with owners and target phases: Phase 3 must HTML-escape `problema` / `solucao` / `resultado` when rendering, and Phase 6 must JSON-encode the `faq` strings into the `FAQPage` script. They are Low because D-11 / D-09 keep every JSON-LD-bound field plain text.
    Fill the findings table with `P02-00x` IDs using the `Low` / `Med` / `High` vocabulary. **No finding may be left High and Open** — that is ROADMAP Phase 2 success criterion 4. If a High appears, stop and report rather than filing the run.
    Do not modify `scripts/security-check.sh` or `.planning/security/SECURITY-CHECKLIST.md`. Do not append a sign-off line — Felipe signs off at the plan 02-04 checkpoint.
  </action>
  <verify>
    <automated>pnpm build && bash scripts/security-check.sh --ci</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/security-check.sh --ci` exits 0 and its final line is `== resumo: 5 PASS / 0 FAIL / 2 SKIP ==`
    - `.planning/security/runs/phase-02.md` exists
    - `grep -c "^PASS: verificacao [1-5]" .planning/security/runs/phase-02.md` outputs `5`
    - `grep -c "^SKIP: verificacao [6-7]" .planning/security/runs/phase-02.md` outputs `2`
    - `grep -n "resumo: 5 PASS / 0 FAIL / 2 SKIP" .planning/security/runs/phase-02.md` matches
    - `grep -c "P02-0" .planning/security/runs/phase-02.md` is at least 1 (findings table populated)
    - `grep -n "| High | Open |\|High.*Open" .planning/security/runs/phase-02.md` prints nothing (no open High finding)
    - `grep -in "nenhuma dependência adicionada\|nenhuma dependencia adicionada" .planning/security/runs/phase-02.md` matches
    - `grep -rnE "re_[A-Za-z0-9_-]{20,}" .planning/security/runs/phase-02.md` prints nothing (no secret value pasted)
    - `git status --porcelain scripts/security-check.sh .planning/security/SECURITY-CHECKLIST.md` prints nothing (neither file modified)
    - `git diff --name-only -- package.json pnpm-lock.yaml` prints nothing
  </acceptance_criteria>
  <done>`.planning/security/runs/phase-02.md` holds the verbatim `security-check.sh --ci` output (5 PASS / 0 FAIL / 2 SKIP), answers all seven gabarito items and all four judgement items, and its `P02-00x` findings table has no High left Open.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| content file → build pipeline | The probes in Task 1 deliberately cross this boundary with malformed input to confirm the gate rejects it. |
| build output → visitor | `security-check.sh` checks 3, 4 and 5 inspect exactly what would be served: inline CSS/script surface, secret leakage, and serverless Function count. |
| package registry → build | Check 1 (`pnpm audit --audit-level=high`) re-asserts the supply-chain baseline on the unchanged lockfile. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-11 | Tampering | the strict-schema gate itself | mitigate | Task 1 executes two adversarial probes (unknown key, missing required field) and requires both to produce a non-zero `pnpm build`. A silently-non-strict schema — the realistic failure mode, e.g. `z.object` instead of `z.strictObject` — is caught here rather than in Phase 3. |
| T-02-12 | Tampering | probe residue left in the working tree | mitigate | Both probes are reverted with `git checkout --` inside the same verify command, and `git status --porcelain src/` must print nothing afterwards. No probe artifact, temp file or log is committed. |
| T-02-13 | Information Disclosure | the SEC-07 run file | mitigate | `VERCEL_AUTOMATION_BYPASS_SECRET` is never echoed by the script and never pasted into the run file; acceptance criteria grep the file for the `re_[A-Za-z0-9_-]{20,}` pattern and require zero matches. Check 4 independently scans the build output for declared secret names. |
| T-02-14 | Repudiation | security review record | mitigate | The run file pins the reviewed `main` HEAD SHA, the exact commands, and the verbatim stdout, so the review is reproducible against a specific commit rather than a claim. |
| T-02-15 | Elevation of Privilege | gate scripts modified to pass | mitigate | `scripts/security-check.sh` and `.planning/security/SECURITY-CHECKLIST.md` are explicitly out of scope; acceptance criteria assert `git status --porcelain` is empty for both, so the gate cannot be weakened to make the phase green. |
| T-02-16 | Denial of Service | check 6 / check 7 skipped locally | accept | Without a phase-02 preview URL, the header baseline and the Lighthouse gate SKIP locally. Compensating control: the CI `lhci` job runs against the Vercel preview and is a required status check on `main`; header assertions do not begin until Phase 7 per the gabarito. Recorded explicitly in the run file rather than hidden. |
| T-02-SC | Tampering | npm/pnpm installs | mitigate | **Zero packages installed across the whole phase.** `pnpm install --frozen-lockfile` proves the lockfile resolves unchanged, check 1 runs `pnpm audit --audit-level=high`, and `git diff` on `package.json` / `pnpm-lock.yaml` must be empty. 02-RESEARCH.md § "Package Legitimacy Audit" records N/A. No `[ASSUMED]` / `[SUS]` package exists, so no legitimacy checkpoint is required. |

No High-severity threat is introduced by this plan. If the SEC-07 run surfaces a High finding, the phase does not close — that is the hard rule in `.planning/security/SECURITY-CHECKLIST.md`.
</threat_model>

<verification>
- Negative probe A (unknown key) exits non-zero; probe B (missing `order`) exits non-zero; clean `pnpm build` exits 0
- `git status --porcelain src/` prints nothing after the probes
- `bash scripts/security-check.sh --ci` exits 0 with `== resumo: 5 PASS / 0 FAIL / 2 SKIP ==`
- `.planning/security/runs/phase-02.md` has no High finding in Open status
- `git status --porcelain scripts/ package.json pnpm-lock.yaml` prints nothing
</verification>

<success_criteria>
- ROADMAP Phase 2 success criterion 1 is evidenced: a bad key AND a missing field each fail `pnpm build`, with verbatim output recorded
- ROADMAP Phase 2 success criterion 4 has its artifact: a SEC-07 phase-02 run with no open High finding, "nenhuma dependência adicionada" answered, and no new inline surface
- The working tree and the gate scripts are unmodified by the verification itself
</success_criteria>

<output>
Create `.planning/phases/02-content-collections/02-03-SUMMARY.md` when done.
</output>
