---
phase: 04-progressive-enhancement-effects
plan: 04
type: execute
wave: 4
depends_on: ["04-03"]
files_modified:
  - .planning/security/runs/phase-04.md
autonomous: false
requirements: [ANIM-01, ANIM-03, ANIM-04, ANIM-05, ANIM-06, ANIM-09]

must_haves:
  truths:
    - "Decisões do CONTEXT.md cobertas por este plano: D-05, D-06, D-08, D-09"
    - "Está provado, por medição e não por leitura de código, que o loop do canvas para com o hero fora da tela e com a aba oculta"
    - "Está provado que sob prefers-reduced-motion o canvas desenha e para, e que o glow não renderiza"
    - "Está provado que o DPR efetivo do canvas não passa de 1,5 mesmo em tela de alta densidade"
    - "Lighthouse mobile no preview fica >= 95 em Performance, Accessibility e Best Practices, com TBT < 200 ms e CLS < 0,05"
    - "A fase fecha sem nenhum achado High em aberto e com assinatura do Felipe"
  artifacts:
    - path: ".planning/security/runs/phase-04.md"
      provides: "Arquivo de execução SEC-07 da fase 4 com os sete itens do gabarito e a tabela de achados"
      contains: "phase-04"
  key_links:
    - from: ".planning/security/runs/phase-04.md"
      to: ".planning/security/SECURITY-CHECKLIST.md"
      via: "os sete itens mecânicos do gabarito respondidos um a um"
      pattern: "verificacao"
    - from: ".planning/security/runs/phase-04.md"
      to: "os blocos threat_model dos planos 04-01 a 04-03"
      via: "IDs T-04-xx refletidos na tabela de achados ou declarados mitigados"
      pattern: "T-04-"
---

<objective>
Fechar a Fase 4 contra medição real, não contra leitura de código: provar que o loop do
canvas efetivamente para, que o reduced-motion efetivamente desliga tudo, que o Lighthouse
mobile não regrediu, registrar a execução SEC-07 e obter o aceite do Felipe.

Purpose: o critério de sucesso 5 do ROADMAP é literalmente "DevTools não mostra atividade
de script com o hero fora da tela" — um comportamento que nenhuma asserção de build
alcança. 04-VALIDATION.md lista cinco verificações inerentemente manuais, e
04-RESEARCH.md Open Question 1 avisa que o custo O(n²) do canvas dentro da janela de
medição do Lighthouse só pode ser conhecido medindo contra output real.
Output: evidência instrumentada capturada, `.planning/security/runs/phase-04.md` e a
assinatura do Felipe.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-progressive-enhancement-effects/04-VALIDATION.md
@.planning/phases/04-progressive-enhancement-effects/04-RESEARCH.md
@.planning/phases/04-progressive-enhancement-effects/04-CONTEXT.md
@.planning/security/SECURITY-CHECKLIST.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verificação instrumentada dos três efeitos via Chrome DevTools Protocol</name>
  <files>(sem alteração de arquivo versionado — script de scratchpad, nunca commitado)</files>
  <read_first>
    - .planning/phases/03-static-zero-js-sections-csp-safe-refactor-a11y/03-08-SUMMARY.md, seção de método de medição (a receita de CDP já estabelecida neste projeto: Chrome headless com `--remote-debugging-port`, dirigido de um script Node de scratchpad usando o `WebSocket` nativo do Node >= 22, domínios `Emulation.setDeviceMetricsOverride`, `Page.navigate`, `Runtime.evaluate`; nenhuma dependência nova; processos encerrados ao final)
    - .planning/phases/04-progressive-enhancement-effects/04-VALIDATION.md, seção "Manual-Only Verifications" (as cinco verificações e o que exatamente cada uma precisa provar)
    - .planning/phases/04-progressive-enhancement-effects/04-CONTEXT.md, decisões D-05, D-06, D-08, D-09
    - .planning/STATE.md, decisão da Fase 1 sobre a verificação 7 local falhar nesta máquina (chrome-launcher EPERM mais antivírus) — motivo pelo qual o Chrome é lançado diretamente, não via chrome-launcher, e pelo qual o gate autoritativo do Lighthouse é o job `lhci` do CI
    - src/scripts/particles.ts, src/scripts/glow.ts, src/scripts/reveal.ts (o que exatamente está sendo medido)
  </read_first>
  <action>
    Subir o build da fase localmente (`pnpm build` seguido de `astro preview`, ou
    `pnpm dev` se o preview não servir os assets corretamente) e lançar uma instância
    headless do Chrome já instalado na máquina com `--remote-debugging-port`. Dirigir tudo
    de um script Node no diretório de scratchpad da sessão — NÃO commitar esse script, NÃO
    instalar nenhum pacote (usar o `WebSocket` nativo, exatamente como o plano 03-08 fez).

    Capturar a saída verbatim de cada medição abaixo para o SUMMARY e para o arquivo SEC-07
    da Task 2. Se o Chrome não subir nesta máquina, PARE e reporte: nesse caso estas
    verificações passam para o checkpoint humano da Task 3 com instruções de DevTools, e
    isso precisa ficar registrado como desvio, não silenciosamente pulado.

    MEDIÇÃO A — `.js-ready` e reveal (ANIM-01). Depois de `Page.navigate` e load:
    · `document.documentElement.classList.contains('js-ready')` deve ser `true`
    · contar `document.querySelectorAll('[data-reveal].is-revealed').length` logo após o
      load (deve ser > 0 e < o total, já que só o que está acima da dobra disparou) e
      novamente depois de 3000 ms sem rolar (deve ser igual ao total de `[data-reveal]`,
      provando o fallback de 2600 ms). Registrar os três números: revelados no load,
      revelados aos 3 s, total.

    MEDIÇÃO B — gate do glow (ANIM-03, D-05). Ler
    `getComputedStyle(document.querySelector('.cursor-glow')).display` em três condições:
    · padrão desktop (ponteiro fino, sem reduced-motion) → esperado `block`
    · `Emulation.setEmulatedMedia` com a feature `prefers-reduced-motion: reduce` →
      esperado `none`
    · `Emulation.setDeviceMetricsOverride` com `mobile: true` e emulação de toque
      (ponteiro grosso) → esperado `none`
    Nas duas condições em que o esperado é `none`, confirmar também que nenhum listener de
    `pointermove` foi registrado — checar que o `transform` computado do elemento continua
    sendo o estado inicial de estacionamento fora da viewport após despachar movimentos de
    ponteiro sintéticos via `Input.dispatchMouseEvent`.

    MEDIÇÃO C — pausa do loop do canvas (ANIM-04). Usar `Performance.enable` e
    `Performance.getMetrics`, lendo a métrica `ScriptDuration` como delta entre dois pontos
    no tempo, em três janelas de 3 segundos cada:
    · hero visível, aba visível → delta de `ScriptDuration` claramente > 0 (é a linha de
      base: o loop está rodando)
    · depois de `window.scrollTo(0, document.body.scrollHeight)` e aguardar o
      IntersectionObserver → delta esperado próximo de zero
    · de volta ao topo, com a aba "oculta": redefinir o getter de `document.hidden` para
      `true` via `Object.defineProperty` e despachar `visibilitychange` → delta esperado
      próximo de zero
    Critério: cada uma das duas janelas pausadas deve ficar abaixo de 20% do delta da
    janela de linha de base. Registrar os três deltas em milissegundos. Se alguma janela
    pausada não cair, o gate duplo está quebrado (provável anti-padrão dos dois caminhos
    independentes de start/stop) — é achado bloqueante, não ajuste cosmético.

    MEDIÇÃO D — frame estático sob reduced-motion (ANIM-05, D-09). Com
    `prefers-reduced-motion: reduce` emulado, recarregar e provar as duas metades do
    contrato:
    · o canvas DESENHOU: somar os bytes de
      `document.querySelector('.hero-canvas').getContext('2d').getImageData(0, 0, 40, 40).data`
      e confirmar que é maior que zero em ao menos uma amostra da área do canvas
    · e PAROU: delta de `ScriptDuration` numa janela de 3 s próximo de zero
    Registrar os dois números.

    MEDIÇÃO E — cap de DPR (ANIM-05, D-06). Com
    `Emulation.setDeviceMetricsOverride` usando `deviceScaleFactor: 3`, recarregar e
    avaliar
    `document.querySelector('.hero-canvas').width / document.querySelector('.hero-canvas').getBoundingClientRect().width`.
    Esperado: exatamente `1.5` (o cap), nunca 2 nem 3. Registrar o valor.

    MEDIÇÃO F — ANIM-06, regressão de keyframes. Confirmar por grep que esta fase não
    introduziu nenhum consumidor novo de `dmFloat`/`dmPulse` fora do guarda-chuva de
    reduced-motion da Fase 3, e que `src/styles/base.css` está inalterado desde a Fase 3.

    Encerrar o Chrome headless e o servidor local ao final, como o plano 03-08 fez.
  </action>
  <verify>
    <automated>bash -c 'set -e; pnpm test; pnpm check; pnpm build; bash scripts/js-weight-check.sh; git diff --quiet HEAD -- src/styles/base.css src/components/HeroBleed.astro; ! grep -rn "dmFloat\|dmPulse" src/scripts/ src/layouts/'</automated>
    <human-check>As seis medições A a F foram executadas e seus números estão transcritos verbatim no SUMMARY</human-check>
  </verify>
  <acceptance_criteria>
    - Medição A: `js-ready` presente no `<html>`; a contagem de `[data-reveal].is-revealed` no load é maior que zero e menor que o total, e aos 3 s é igual ao total
    - Medição B: `display` computado do `.cursor-glow` é `block` no perfil desktop e `none` nas duas condições (reduced-motion e ponteiro grosso), com os três valores registrados
    - Medição B: nas duas condições de gate fechado, o `transform` do `.cursor-glow` permanece no estado inicial mesmo após eventos de ponteiro sintéticos
    - Medição C: os três deltas de `ScriptDuration` estão registrados em ms e cada janela pausada é menor que 20% da janela de linha de base
    - Medição D: a amostra de `getImageData` do canvas soma mais que zero (desenhou) E o delta de `ScriptDuration` da janela de 3 s é próximo de zero (parou)
    - Medição E: a razão `canvas.width / rect.width` com `deviceScaleFactor: 3` é exatamente `1.5`
    - Medição F: `git diff --quiet HEAD -- src/styles/base.css src/components/HeroBleed.astro` sai 0 e nenhum arquivo em `src/scripts/` ou `src/layouts/` menciona `dmFloat`/`dmPulse`
    - Nenhum arquivo de script de medição foi commitado e nenhum pacote foi instalado
    - Os processos de Chrome headless e do servidor local foram encerrados
  </acceptance_criteria>
  <done>Os três efeitos foram provados por medição instrumentada, incluindo as duas condições de pausa e o caminho de reduced-motion, com números registrados.</done>
</task>

<task type="auto">
  <name>Task 2: Preview real, gate do Lighthouse e arquivo de execução SEC-07</name>
  <files>.planning/security/runs/phase-04.md</files>
  <read_first>
    - .planning/security/SECURITY-CHECKLIST.md (o gabarito: regra dura de nenhum High em aberto, os sete itens mecânicos mapeados às verificações do script, e o formato da tabela de achados D-12)
    - .planning/security/runs/phase-03.md (exemplo mais recente de preenchimento; os achados P03-xxx abertos que precisam ser carregados adiante)
    - .planning/security/runs/phase-01.md (achados P01-xxx, incluindo as duas advisories de `extract-zip` aceitas com prazo, e a frase de deferimento do INP de PERF-02)
    - scripts/security-check.sh (como `PREVIEW_URL` e `VERCEL_AUTOMATION_BYPASS_SECRET` tiram as verificações 6 e 7 do estado SKIP)
    - .planning/STATE.md, decisões da Fase 1: Deployment Protection em previews, header `x-vercel-protection-bypass`, `categories:seo` em `warn` até a Fase 6, e a regra de nunca usar a forma stdin de `gh secret set` nesta máquina
    - Os blocos `<threat_model>` dos planos 04-01, 04-02 e 04-03 (os IDs T-04-01 a T-04-15 e T-04-SC precisam estar refletidos na tabela de achados ou declarados mitigados)
    - .planning/phases/04-progressive-enhancement-effects/04-RESEARCH.md, seção "SEC-07 mapping for this phase" (quais das sete verificações esta fase toca: a 2 não é acionada por escrita de `.style` em runtime; a 3 não muda; a 7 é a que pode regredir de verdade) e Pitfall 4 (o clamp de `hardwareConcurrency` em iOS/Firefox é comportamento esperado, não defeito)
  </read_first>
  <action>
    Abrir um pull request com o trabalho da fase (ou reutilizar o já aberto) para que a
    Vercel produza um preview deploy. Esperar ficar READY, exportar a URL em `PREVIEW_URL`
    e lembrar que toda requisição precisa do header `x-vercel-protection-bypass` com o
    valor de `VERCEL_AUTOMATION_BYPASS_SECRET`.

    Item A — gates 6 e 7. Rodar `bash scripts/security-check.sh --ci` com `PREVIEW_URL`
    definido e capturar a saída completa mais a linha
    `== resumo: N PASS / N FAIL / N SKIP ==` e o código de saída.

    Item B — Lighthouse mobile. O gate autoritativo é o job `lhci` do CI contra o preview
    (decisão da Fase 1). Aguardar o job no PR e capturar as quatro pontuações medianas mais
    LCP, CLS e TBT. Exigências desta fase: Performance, Accessibility e Best Practices em
    0.95 ou mais; **TBT abaixo de 200 ms** e **CLS abaixo de 0,05**. SEO continua em `warn`
    até a Fase 6. Atenção especial ao TBT: é a primeira fase com JavaScript, o loop do
    canvas começa dentro da janela de medição, e o RESEARCH Open Question 1 prevê que este
    é o número mais provável de regredir.

    Se o TBT estourar: a ordem de ataque é (1) micro-otimizar o `tick` — confirmar que
    `Math.sqrt` e `globalAlpha` estão em uso e que nada aloca por frame; (2) reportar ao
    Felipe. NÃO baixar o teto de 90 pontos nem alterar a fórmula de D-06 por conta própria:
    é decisão travada, e quebrá-la em silêncio é exatamente o que o RESEARCH manda não
    fazer.

    Item C — peso de JS contra o preview. Confirmar que `bash scripts/js-weight-check.sh`
    continua passando e registrar o total gzipado e a folga contra os 20480 B.

    Depois, criar `.planning/security/runs/phase-04.md` seguindo exatamente o formato de
    `phase-03.md` — mesmo cabeçalho, mesma ordem, mesma tabela, em pt-BR. Conteúdo
    obrigatório:

    1. Saída verbatim de `scripts/security-check.sh --ci` com `PREVIEW_URL` definido,
       incluindo a linha de resumo e o código de saída.
    2. Os sete itens do gabarito respondidos um a um. Pontos específicos desta fase:
       · `pnpm audit` limpo — esta fase instala ZERO pacotes; `pnpm-lock.yaml` inalterado.
       · Verificação 2 (`style=` em `src/`): registrar explicitamente que esta fase escreve
         `element.style.transform` em RUNTIME (glow) e que isso não aciona o grep, que só
         procura o literal `style="` em arquivos-fonte. Nenhum atributo `style=` autoral
         foi adicionado.
       · Verificação 3 (CSS inline no HTML construído): inalterada — as alterações de CSS
         desta fase são um bloco escopado no `BaseLayout.astro` e uma linha em
         `tokens.css`, ambos externalizados por `build.inlineStylesheets: 'never'`.
       · Superfície NOVA de script: esta é a primeira fase com JavaScript no projeto.
         Declarar as duas tags e por que cada uma tem a forma que tem — o
         `<script is:inline>` do `.js-ready` (conteúdo estático de uma instrução,
         auto-hashável pela `security.csp` da Fase 7) e o `<script>` módulo empacotado
         (recurso same-origin coberto por `script-src 'self'`). Declarar também, com
         evidência de grep, que o HTML construído não contém `eval(`, `new Function(`,
         nem atributo de handler inline.
       · Varredura de segredos: nenhum segredo introduzido.
       · Contagem de Functions: continua no máximo 1; esta fase não cria rota dinâmica.
       · Gate do Lighthouse com os números do job `lhci`, incluindo TBT e CLS explícitos.
    3. Tabela de achados no formato D-12, numerada `P04-001` em diante, refletindo as
       disposições dos três `<threat_model>` desta fase. No mínimo: os riscos aceitos
       T-04-03 e T-04-13 (hints de dispositivo imprecisos / clamp de `hardwareConcurrency`
       em iOS Safari e Firefox, que faz todo visitante de iPhone receber metade das
       partículas — comportamento esperado por D-07, não defeito) e T-04-15
       (`ResizeObserver` sem debounce). Carregar adiante os achados abertos das fases
       anteriores que ainda se aplicam (as duas advisories de `extract-zip` e o que restar
       aberto em `phase-03.md`).
    4. Registrar a decisão de planejamento que divergiu do PATTERNS: o token novo
       `--z-cursor-glow: 20`, e por quê (todas as seções da Fase 3 são
       `z-index: var(--z-section)` com fundo opaco, então um glow em `--z-glow: 5` ficaria
       invisível). Não é achado de segurança, mas é decisão de arquitetura que a Fase 7
       precisa conhecer.
    5. Declaração explícita: nenhum achado High em aberto. Se houver High, a fase NÃO fecha
       — reportar em vez de escrever a declaração.
  </action>
  <verify>
    <automated>bash -c 'set -e; F=.planning/security/runs/phase-04.md; test -f "$F"; grep -q "resumo:" "$F"; grep -q "P04-001" "$F"; grep -qi "nenhum achado High" "$F"; grep -qi "lighthouse" "$F"; grep -qi "TBT" "$F"; grep -q "is:inline" "$F"; grep -q -- "--z-cursor-glow" "$F"; grep -q "T-04-" "$F"'</automated>
  </verify>
  <acceptance_criteria>
    - `PREVIEW_URL` aponta para um deploy READY da Vercel referente ao head desta fase
    - `bash scripts/security-check.sh --ci` com `PREVIEW_URL` roda as verificações 6 e 7 sem SKIP e a saída está capturada verbatim no SUMMARY e no arquivo SEC-07
    - O job `lhci` do PR reporta Performance, Accessibility e Best Practices em 0.95 ou mais, TBT abaixo de 200 ms e CLS abaixo de 0,05; os sete números estão registrados
    - `.planning/security/runs/phase-04.md` existe e segue a estrutura de `phase-03.md`
    - Os sete itens do gabarito estão respondidos, cada um com evidência, nenhum em branco
    - O arquivo declara explicitamente a nova superfície de script (as duas tags, `is:inline` versus módulo empacotado) e a evidência de ausência de `eval(`/`new Function(`/handler inline no HTML construído
    - A tabela de achados começa em `P04-001` e inclui ao menos os riscos aceitos T-04-03, T-04-13 e T-04-15, além dos achados herdados ainda abertos
    - A decisão do token `--z-cursor-glow` está registrada com justificativa
    - Há uma declaração literal de que não há achado High em aberto
  </acceptance_criteria>
  <done>O preview real comprovou o Lighthouse dentro do gate e a execução SEC-07 da fase está versionada, com evidência verbatim e achados numerados.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Aceite do Felipe — movimento, reduced-motion e sensação geral</name>
  <what-built>
    Os três comportamentos de movimento do arquivo de design, ligados ao site estático que
    a Fase 3 entregou:

    · REVEAL AO ROLAR — cada bloco das nove seções agora sobe 20px e aparece quando entra
      na tela, uma vez só. Se o navegador não suportar o mecanismo, ou se algo travar,
      tudo aparece sozinho em 2,6 segundos.
    · GLOW DO CURSOR — um halo roxo de 640px que acompanha o mouse pela página inteira,
      em cima do conteúdo, com modo de mistura "screen". Só existe em computador com
      mouse.
    · CANVAS DE PARTÍCULAS — a rede de pontos e linhas do hero agora anima, e para
      completamente quando você rola o hero para fora da tela ou troca de aba.

    Com "reduzir movimento" ligado no sistema: o glow não existe, o canvas desenha um
    único quadro parado e as seções aparecem já visíveis, sem transição.

    O que NÃO está aqui, por decisão: o formulário ainda não envia nada (Fase 5); não há
    título, meta description nem dados estruturados (Fase 6); os cabeçalhos de segurança e
    a CSP estrita entram na Fase 7. As imagens do hero e do retrato continuam sendo
    placeholders vetoriais.

    Um ponto de expectativa: sobre as seções CLARAS (Diferenciais e Contato) o glow fica
    quase invisível. Isso é proposital e foi decidido por você (D-04) — o modo "screen" é
    incondicional, sem exceção por seção.
  </what-built>
  <action>
    Apresentar ao Felipe o checkpoint abaixo com a URL do preview da Task 2 em mãos. Não
    prosseguir, não marcar a fase como concluída e não rodar o fechamento de fase sem a
    resposta explícita dele. Depois da resposta, transcrevê-la literalmente no SUMMARY e
    anexar uma linha de assinatura ao final de `.planning/security/runs/phase-04.md`, no
    mesmo formato usado em `phase-03.md`. Se ele reprovar qualquer item, registrar o item
    como achado numerado `P04-xxx` no arquivo de execução SEC-07 e manter a fase aberta.
  </action>
  <how-to-verify>
    Abra o preview no navegador (URL no SUMMARY da Task 2), em um computador com mouse, e
    confira na ordem:

    1. GLOW. Mova o mouse pela página. Um halo roxo acompanha o cursor? Ele passa por cima
       do texto das seções escuras (esse é o comportamento pretendido, não um bug)? Nas
       seções claras "Diferenciais" e "Contato" ele some quase por completo — está
       aceitável para você, como combinado em D-04?

    2. GLOW NO CANTO. Recarregue a página SEM mexer no mouse. Aparece alguma mancha roxa no
       canto superior esquerdo antes do primeiro movimento? A resposta esperada é NÃO — o
       halo fica estacionado fora da tela até o primeiro movimento. Se aparecer, é defeito.

    3. REVEAL. Role a página devagar do topo ao rodapé. Cada bloco aparece subindo
       suavemente, uma vez só? Algum bloco fica invisível, aparece tarde demais, ou
       "pisca"?

    4. FLASH NO CARREGAMENTO. Recarregue algumas vezes, de preferência com a rede
       estrangulada (DevTools > Network > Slow 4G). Em algum momento o conteúdo aparece
       inteiro e depois some para então reaparecer? A resposta esperada é NÃO — é
       exatamente o problema que o script minúsculo no `<head>` existe para evitar.

    5. CANVAS DO HERO. As partículas se movem e se conectam por linhas? A densidade parece
       certa — nem vazio demais, nem poluído?

    6. PAUSA. Role o hero para fora da tela e deixe a página parada por alguns segundos.
       Depois volte ao topo. As partículas voltam a animar normalmente? Troque para outra
       aba por uns 10 segundos e volte: idem? (A prova de que a CPU realmente parou está
       medida no SUMMARY da Task 1 — aqui você só confirma que nada quebra ao retomar.)

    7. MOVIMENTO REDUZIDO. Ative "Reduzir movimento" no Windows (Configurações >
       Acessibilidade > Efeitos visuais > Efeitos de animação, desligar) e recarregue.
       Confirme: o halo do cursor NÃO aparece de jeito nenhum; o canvas do hero mostra uma
       rede de pontos PARADA (não vazia); as seções já aparecem visíveis, sem transição; a
       página continua parecendo acabada, não "sem graça" nem quebrada.

    8. SEM JAVASCRIPT. DevTools > F1 > "Disable JavaScript" > recarregar. A página inteira
       continua visível, sem nenhuma seção invisível ou deslocada? (Sem JS não há halo nem
       partículas — isso é o esperado.)

    9. CELULAR. Abra o preview no seu telefone. O halo do cursor NÃO deve existir. As
       seções aparecem ao rolar normalmente. O hero continua animando, com menos pontos que
       no computador. Nada engasga ao rolar?

    10. SEC-07. Leia `.planning/security/runs/phase-04.md` e confirme que a declaração de
        "nenhum achado High em aberto" está correta e que os riscos aceitos são aceitáveis
        para você — em especial: todo visitante de iPhone recebe metade das partículas
        (limitação de privacidade do iOS Safari, não defeito), e o halo do cursor fica
        lavado nas seções claras.

    Se algo estiver errado, descreva o item pelo número — não é preciso diagnosticar a
    causa.
  </how-to-verify>
  <resume-signal>
    Responda "aprovado" para fechar a Fase 4, ou descreva por número os itens com problema.
    Um "aprovado" aqui também vale como a assinatura SEC-07 da fase.
  </resume-signal>
  <acceptance_criteria>
    - Felipe respondeu explicitamente neste checkpoint
    - A resposta está registrada literalmente no SUMMARY e uma linha de assinatura foi anexada a `.planning/security/runs/phase-04.md`
    - Se houve reprovação de qualquer item, ele está registrado como achado numerado (`P04-xxx`) ou como tarefa de fechamento de lacuna, e a fase NÃO é marcada como concluída
  </acceptance_criteria>
  <done>Felipe aprovou o comportamento dos três efeitos, o caminho de reduced-motion e o comportamento sem JS, e a execução SEC-07 está assinada.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| internet pública -> deploy de preview da Vercel | superfície exposta, protegida por Deployment Protection |
| HTML e bundle construídos -> CDN | conteúdo imutável distribuído ao público |
| build desta fase -> política de CSP da Fase 7 | a forma das tags de script precisa estar registrada antes de a CSP ser escrita |
| fechamento de fase -> registro auditável | sem evidência versionada não há como provar o que foi verificado |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-16 | Repudiation | fechamento da Fase 4 | mitigate | SEC-07 exige arquivo de execução versionado com saída verbatim, achados numerados `P04-xxx` e assinatura humana; a regra dura "nenhuma fase fecha com High em aberto" é reafirmada no arquivo e no checkpoint |
| T-04-17 | Tampering | superfície de script no HTML publicado (ASVS V14) | mitigate | A verificação contra o preview real grepa o HTML servido em busca de `eval(`, `new Function(` e atributos de handler inline, e o arquivo SEC-07 declara nominalmente as duas únicas tags `<script>` e a forma de cada uma, para que a Fase 7 escreva `script-src 'self'` sem retrabalho |
| T-04-18 | Denial of Service | orçamento de CPU do visitante (TBT) | mitigate | Gate do Lighthouse com limiar explícito de TBT < 200 ms medido contra o preview real, mais a prova instrumentada da Task 1 de que o loop para em ambas as condições de pausa. Regressão é bloqueante, com ordem de ataque definida que proíbe quebrar a fórmula travada de D-06 |
| T-04-19 | Information Disclosure | cabeçalhos do preview | mitigate | Verificação 6 do `security-check.sh` executa `curl -I` contra o preview como checagem de regressão de linha de base; o conjunto final de cabeçalhos e a CSP estrita são escopo da Fase 7 |
| T-04-SC | Tampering | árvore de dependências | mitigate | `pnpm audit --audit-level=high` roda dentro de `security-check.sh` e no CI; esta fase não instalou nenhum pacote e o `pnpm-lock.yaml` está inalterado. As advisories de `extract-zip` herdadas continuam como achados Med aceitos com prazo |
</threat_model>

<verification>
- Seis medições instrumentadas (A a F) executadas, com números verbatim no SUMMARY.
- `scripts/security-check.sh --ci` com `PREVIEW_URL` executa as 7 verificações sem SKIP.
- Job `lhci`: Performance, Accessibility e Best Practices >= 0.95; TBT < 200 ms; CLS < 0,05.
- `.planning/security/runs/phase-04.md` existe, sem High em aberto, assinado.
</verification>

<success_criteria>
- Todas as verificações manuais listadas em 04-VALIDATION.md foram executadas e registradas, por medição instrumentada onde possível e por inspeção humana onde não.
- O gate de performance passou contra o preview real, com TBT e CLS explícitos.
- A execução SEC-07 da fase está versionada e assinada.
- Felipe aprovou explicitamente, ou os problemas viraram achados numerados com a fase mantida aberta.
</success_criteria>

<output>
Create `.planning/phases/04-progressive-enhancement-effects/04-04-SUMMARY.md` when done
</output>
